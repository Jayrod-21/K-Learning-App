# Backlog Reconciliation 3 — Wave-2 page-batch family (F-143…F-190, B-036)

Verified against `rebuild` @ branch `docs/backlog-reconcile-2026-07-15` (2026-07-15).
Method: read each ticket's full entry in `BUGS_AND_FEATURES.md`, grep the code paths
named in its "Key files"/"Where", read the implementing code directly, and cross-check
against `git log --oneline -50` + the `docs/redesign/REVIEW_*` / `FIX_REPORT_*` trail.
A ticket ID appearing in a commit message or code comment is treated as a hint, not
proof — every row below cites the actual code (or its absence) that was inspected.

This batch covers the per-page Wave-2 "Seoul Day & Night" redesign backlog: Progress,
Library (Vocab/Grammar/Mistakes/Uploads), Flashcards, Grammar practice, TOPIK, Listen,
Writing, Hanja, plus the Wave-2 fix-pass follow-up tickets and the Phone-round-4 batch
(B-036, F-187–F-190) that shipped in PR #109 minutes before this pass started.

| Ticket | Verdict | Evidence (commit/file:line) | Notes |
|---|---|---|---|
| F-143 | DONE | `client/src/pages/Progress.tsx:59-62,641-646` — explicit comment: the "begin today's plan"/"gaps-next-steps" copy never existed on Progress (it lives on `Diagnostic.tsx`'s results screen); `Progress.test.tsx` pins a regression that the strings never appear. Commit `9243489`. | Resolved as a verified no-op, not a feature add. |
| F-144 | DONE | `client/src/pages/review/ReviewVocab.tsx:303` (`kinds={['vocab']}`); `client/src/components/MyVocabLists.tsx:202` (`visibleLists` kind-filter), `:434` (kind-picker hidden when `kinds.length<=1`); `.km-grammar__state`→`.km-vocab__state` rename. Commit `c15ade3`. | Fixed at 3 layers (creatable-kind, list filter, classname). No `km-grammar__*` UI remains on Vocab. |
| F-145 | DONE | `client/src/pages/review/ReviewVocab.css:16` (`overflow-x:hidden` on `.km-vocab`), `:47-51` (`@media (max-width:420px)` stacks filters), `:61-77` (`flex-wrap`+`min-width:0`); header doc cites F-129/F-145. | Vocab-specific responsive pass, paired with the already-DONE F-129 backstop. |
| F-146 | DONE | `ReviewVocab.tsx:289-304` — `<MyVocabLists>` wrapped in `<CollapsibleTile title="My lists">`. | Default-open fold, matches ticket intent. |
| F-147 | DONE | `MyVocabLists.tsx:206-218` ("New list" → `CreateListSheet`, `:338-375`); `ReviewVocab.tsx:832` (`AddToListSheet` hardcodes `kind:'vocab'`), `:769,777` (vocab-only list display). Commit `c15ade3`. | Both create entry points are Sheets and vocab-scoped only. |
| F-148 | DONE | `ReviewVocab.tsx:238` (`weekOpen` state), `:313-324` (trigger button), `:326-360` (`<Sheet>` containing `<WeeklySuggestions showGrammar={false}>`, lazily mounted). | Moved from always-inline card to popup; grammar excluded. |
| F-149 | DONE | `ReviewVocab.tsx:572-574` — visible `<Eyebrow className="km-vocab__searchLabel">` "Search for a word / 단어 검색" above `<SearchBox>`; matching `ariaLabel`. | Real visible caption, not just a placeholder. |
| F-150 | DONE | Server: `server/src/routes/krdict.ts:156,193` — `WHERE (part_of_speech IS NULL OR part_of_speech NOT IN ('어미','조사'))` (both browse+search branches, NULL-safe). Client: `client/src/pages/review/ReviewDictionary.tsx:96-98,289,370` (`isGrammarPos` filter). | Excluded at both server and client layers. Endings/particles removed; 접사 (affix) deliberately kept. |
| F-151 | **OPEN** | `ReviewVocab.tsx:158-160` — `GENRE_OPTIONS` still derives from the unchanged 3-value `DOMAIN_FILTERS` enum; header doc `:141-145` explicitly marks genre-set expansion out of scope. | The literal genre-set expansion was NOT done. A related, richer facet shipped as **F-176** (Theme filter, ~31 values) — if F-151's intent is read loosely as "more ways to slice vocab by category," F-176 partially covers it, but the genre enum itself is unchanged. Genuinely still open. |
| F-152 | DONE | `client/src/pages/review/ReviewGrammar.tsx:24-45,445-477` — labels now Add→Added / **Mastered** (gated on real `graduated_at`, not the add action); no "Bank"/"banked" UI labels remain. Commit `c15ade3` ("honest F-152 mastery"). | Page is `ReviewGrammar.tsx` (route `review/grammar`), not `Grammar.tsx` (that's the separate Grammar-practice page, F-158). |
| F-153 | DONE | `ReviewGrammar.tsx:63-67,99,486-498` — `usePagination(rows, { initial: 15, step: 15 })` + `ShowMore`. Commit `c15ade3`/`2c2d4ad`. | Uncapped by design (no `max`); filter change resets window. |
| F-154 | DONE | `client/src/pages/Mistakes.tsx:21-31,153` — grid of square question-number tiles grouped into date-divided session `CityCard`s; tap opens shared `Sheet` (question / your answer / "See explanation" reveal + Ask-about handoff). Commit `2c2d4ad`. | "Jump-to-explanation" is an in-sheet reveal, not a separate route (none exists) — a deliberate, documented scope choice, not a gap. |
| F-155 | DONE | `client/src/pages/UploadViewer.tsx:107-168` — real Pointer-Events + non-passive-touch swipe-to-turn, plus arrow buttons. Commits `5621256` ("PDF native-touch swipe+bottom arrows"), `c6a4436`. | Confirmed on the actual PDF/Uploads viewer, not just the shared carousel component (F-130). |
| F-156 | DONE | `client/src/pages/Review.tsx:268-273` — `SEED_LIMIT = 15` (comment documents the old value was 100×2 corpora=200; now 15×2=30 max). Commit `8eae3c8`. | |
| F-157 | DONE | `Review.tsx:789-790,813-821,869,960-1032` — "New list" trigger → `CreateListSheet`, sharing `POST /vocab/lists` plumbing. Commit `8eae3c8`. | Was an always-visible inline card; now a popup. |
| F-158 | DONE | `client/src/pages/Grammar.tsx:96-108,481-513` — `formTarget` state + `continuousDrill` flag; form-picker "Drill" action sets a continuous target (advancing never clears it, unlike the one-shot `drillTarget` deep-link). Commit `8eae3c8`. | This is the separate Grammar-**practice** page (route `learn/grammar`), distinct from Library-Grammar (F-152/F-153). |
| F-159 | DONE | `client/src/pages/Topik.tsx:224` (`chooserOpen` state), `:313-314` (`<Sheet open={chooserOpen}>`). Commit `fae8223`. | Gates fresh mount with no explicit `?mode=`; scrim from shared `Sheet` is the "semi-transparent" ask. Explicit-mode deep links skip it. |
| F-160 | **PARTIAL** | Real fix tracked/documented as **F-185** (already in `BUGS_AND_FEATURES.md`, status 🟡 in progress): `tools/ingest/loaders/load_ttmik_audio.py:142` (`_resolve_iyagi_episode_number`, season-block offset) + 8 passing tests. Commit follow-up `3e7d7f8`. | F-160 (client symptom: "audio not playing") and F-185 (root cause: Iyagi season-numbering mapping bug + TTMIK L9 content gap) are the **same underlying issue**. Code is fixed + unit-tested, but (1) the loader re-run against live `km-db` has not happened yet, and (2) 3 episodes (#119/#236/#240) remain a genuine transcript-ingestion gap. Matches the task brief's prediction — genuinely still open/blocked, not a stale-doc case. |
| F-161 | DONE | `client/src/pages/Ttmik.tsx:663,843` (`usePagination`, initial 15 / step 15 / cap 30), `:782-783,911-912` (`canShowMore`/`onShowMore`). `client/src/hooks/usePagination.ts:48-49,79-81`. | Additive "Show more," not a next-page pager — matches ticket ask. |
| F-162 | DONE | `Ttmik.tsx:279-353` (`useListScrollRestore`, sessionStorage-backed, one key per corpus), `:668` (usage). Commit `fae8223`. | Restores once list is ready; persists on every scroll (keyed off `.km-shell__scroll` since window never scrolls). |
| F-163 | DONE | `client/src/pages/Writing.tsx:198,204` (`WritingChoice` includes `'ai_prompt'`, `{ id:'ai_prompt', label:'✦ AI Prompt' }`), `:836,849` (top-level mount, not buried). Commit `fae8223`. | Three-way segmented control: Q53 / Q54 / ✦ AI Prompt. |
| F-164 | DONE | `client/src/pages/Hanja.css:22` — dedicated F-164 spacing section; `Hanja.tsx:52` comment. Commit `8eae3c8`. | |
| F-165 | DONE | `Hanja.tsx` — `promoteState` (:342), `buildDrawQueue` (:354), `DrawView` judge-right/judge-wrong (:2546-2570), Right/Wrong buttons (:2744-2771). Right answer promotes `new→practicing→banked` via the real `Hanja.state` pool (the same one F-167 colors); wrong just re-queues, no write. Commit `8eae3c8`. | **Confirmed genuinely distinct from F-115** (still open): F-165 is a self-judged Anki loop (draw → user taps Right/Wrong, no stroke data needed). F-115 is the still-stubbed *guided + gradable* stroke-order overlay — `Hanja.tsx:2719-2722`'s "About" tile explicitly states stroke-order guidance isn't available (no corpus stroke data). The two do not conflict; F-165 does not depend on F-115. |
| F-166 | DONE | `CreateListSheet` (`Hanja.tsx:1775`), `AddHanjaPicker` (:2225, rendered :2204) — both `Sheet`-based, picker fetches `GET /hanja` pool + seeds/adds. | |
| F-167 | DONE (caveat) | Index grid passes `state={h.state}` to `HanjaCell` (`Hanja.tsx:1016`); colors ride `--km-mastery-*` triad (`HanjaCell.tsx:5-11`, `Hanja.css:98-117`). Commit `5ffe4bb` (batch-3 fix-pass). | Ticket asked green/**yellow/red**; shipped is green(moss)/amber(ochre)/**neutral-gray** for never-studied `new` chars. Batch-3 fix-pass deliberately dropped "red for new" (a never-studied char shouldn't read as "in trouble") and fixed the color to a stable token instead of accent-tracking vermilion. Functionally satisfies "index tiles colored by mastery state" — a documented, deliberate deviation from the literal 3-color ask. |
| F-168 | DONE | Per-tile "+" button (`Hanja.tsx:1026-1035`) → `QuickAddSheet` (:1054+) with `toast({ message: 'Added … to …' })` at :1102/:1106/:1130. | |
| F-169 | DONE — **flag: verify intent before flipping** | `Hanja.tsx:88-91` (own code comment): "the index tiles pass `HanjaCell` only `sound` … never `gloss` (the Korean gloss WORD) … simply omitting the prop is the fix." Confirmed at :1006-1016 — no `gloss` prop passed to the index `HanjaCell`. | The ticket is filed as a **bug** whose title states the symptom ("Index shows hangul sound, not the word") in the same pattern other bug tickets in this doc use to describe the *broken* state (e.g. B-005's title literally describes the bug). The shipped code's own comment frames "show sound-only, omit the gloss" as the deliberate fix — i.e., the implementer read the ticket as "stop cluttering the tile with the gloss word, sound-only is correct," which is a defensible but non-obvious reading of the ticket text. Code matches the literal ticket title either way (as a description of desired OR of buggy state) — flagging as a genuine ambiguity for a human call, not asserting confidently. |
| F-170 | DONE | `SubwayProgress` on the study session (`Hanja.tsx:1454`) and on the draw drill (:2702-2708, live `valueText`="N of M mastered"), both tracking real session position. | |
| F-171 | DONE | Migration `db/migrations/059_hanja_attempts.up.sql`; `server/src/routes/hanja.ts:718-743` (POST logs attempt), `:758-804` (`GET /hanja/attempts`, mirrors `/grammar-drill/attempts`); wired into `client/src/pages/Today.tsx:613-616` (`hanjaDoneToday`). Commits `46358fe`, `6d05e93`. | Real per-attempt table + history endpoint + Today "done today" count, end to end. |
| F-172 | DONE | Migrations `060_reading_attempts`, `061_listening_attempts`; `server/src/routes/reading.ts:632+` (POST/GET `/reading/attempts`), `server/src/routes/ttmik.ts:359+` (`/ttmik/attempts` + `/iyagi/attempts`); Today `Today.tsx:617-624` (`readingDoneToday`, `listeningDoneToday`). | Both corpora covered; Today tiles wired to real per-day counts. |
| F-173 | **PARTIAL** | Backend/plumbing done: `server/src/routes/topik.ts:917-964` resolves `totalItems` on `GET/PUT /topik/attempt`; `client/src/services/topik.ts:190` adds `totalItems?` to `AttemptState`. Missing: `client/src/pages/Today.tsx:97-100` still deliberately omits rendering `SubwayProgress` for the resumed exam; the resume banner (`:633-651`) shows only `answered`, not `answered`-of-`totalItems`. | The data plumbing the ticket asked for exists end-to-end, but the actual UI deliverable (a real "X of N" SubwayProgress on Today for a resumed TOPIK attempt) isn't rendered yet — a stale "not available client-side" comment in Today.tsx confirms this was never revisited after the backend landed. Legitimately still open on the client half. |
| F-174 | DONE | `client/src/components/LineChart.tsx:67` (`trend?: boolean`), `:80-103` (`regressionTrend`, least-squares), `:221,319-336` (gated render). Consumed by `client/src/pages/Progress.tsx:324-332` (all 5 skill-carousel charts pass `trend`). Commit `77e076d`. | Full F-142 parity delivered to the shared component. |
| F-175 | DONE | `server/src/routes/krdict.ts:156,193` — same WHERE clause cited under F-150 (NULL-safe, applied to both the id/exact query and the search query). Commit `0c61d73`. | This ticket IS the server-side half of F-150 (client already excluded grammar) — confirmed exact and complete; pager counts are now accurate. |
| F-176 | DONE | Server: `server/src/routes/vocab.ts:78` (`theme` param), `:141` (WHERE), `:196-211` (`GET /vocab/themes`). Client: `ReviewVocab.tsx:611-616` (Theme `FilterSelect`), fed by `fetchVocabThemes` (:454-457), param sent at :510. | Shipped end-to-end. Ticket suggested the facet live in `lib/libraryFilters.ts`; it instead lives directly in `ReviewVocab.tsx` — a cosmetic location difference, not a functional gap. |
| F-177 | DONE | `client/src/components/PageHubHeader.tsx` exists; Today imports/renders it (`Today.tsx:171,792`); Progress imports/renders it (`Progress.tsx:107,568`, explicit "migrated to the shared PageHubHeader component per F-177" comment at :561). Commit `c15ade3`. | Both pages dropped their inline SkylineHeader+DancheongRail copy. |
| F-178 | DONE | `client/src/lib/skill-colors.ts:90` — `hanja: { hexHue:'ochre', tone:'ochre' }`; Today tiles wire `SKILL_COLOR.*.tone` through `CityCard`'s `tone` prop (`Today.tsx:29` comment: "Hanja=ochre (locked)"). Commit `18b8c2c` (phone round-4, F-189/F-190 batch). | Original ticket asked to migrate off the `plain` fallback onto `ochre` — since superseded by the later canonical per-skill color map (F-189), which also lands on `ochre` for Hanja. Current tone is confirmed `ochre`, satisfying the ask either way. |
| F-179 | **OPEN** | `client/src/components/SwipeCarousel.tsx` — no `onChange`/settled-index prop found (only internal `index` state, line ~128). `client/src/pages/Review.tsx` — no doc comment near `StudySession` (interface at :1474) referencing F-130/F-179/swipe. | Both requested halves (the prop, and the doc comment) are absent. P4 "if ever wanted" priority — genuinely still open, not surprising given its low priority. |
| F-180 | DONE | `Hanja.tsx:720-726` — "Practicing" `StateChip` now reads `tone="ochre"` (was `vermilion`); comment at :755 confirms it reads the same `--km-mastery-practicing` token the index grid uses. `Hanja.test.tsx:1242,1258` pin both the tone and the chip/grid-token match. Commit `5ffe4bb`. | |
| F-181 | DONE | `Hanja.tsx:2546-2558` — `masteredCount` bump is gated `if (next !== current.state)`; a right answer on an already-banked char advances the queue without inflating the label. Explicit F-181 comment at :2551-2554. `Hanja.test.tsx:1171` pins the no-bump regression. | |
| F-182 | DONE | `Hanja.test.tsx:1153` — `'F-182: a right answer on an already-banked character writes nothing (promoteState no-op guard)'`. | Test exists exactly as the ticket asked. |
| F-183 | DONE | `client/src/pages/topik/MockMode.tsx:106` (`CityCard`), `:114` (`SubwayProgress`), `:113` (`SealStamp`), `:829,985` (CityCard signboards) — nested under Topik.tsx's `PageHubHeader`; no legacy flat Topbar. Commit `9243489` + polish `6c6749e`. | |
| F-184 | DONE | `client/src/pages/Images.tsx:69` (import `PageHubHeader`), `:253` (renders it, replacing Topbar), `:458,500,639,699` (CityCard throughout, `tone="plain"`). Commit `9243489`. | Confirmed no `Topbar` usage remains anywhere in the file — last legacy-flat page migrated. |
| F-186 | DONE | `client/src/components/WordPopover.tsx:60` (imports `Sheet`), `:152` (`<Sheet open onClose... tone="accent">`), `:300` (closing tag). Implemented in commit `77e076d` (filed as a follow-up in `0ae7256`). | No bespoke `role="dialog"` JSX remains — only header-comment mentions describing the retired approach. Full a11y now delegated to `Sheet`/`useModalA11y`. |
| B-036 | DONE | Commit `15fcbf3` ("fix(B-036/F-086): migrate font-size px to rem…") — ~375 `font-size:Npx` declarations across 18 CSS files + 13 inline TSX `fontSize` numbers converted to exact px/16 rem equivalents (zero visual change at md root, sm/lg now scale). New guard test `fontSizeUnits.test.ts` asserts no px font-size remains outside 2 documented, intentional exceptions (MockBadge dev-only seal glyph; Images.tsx decorative mock-scene text). | Verified by reading the commit diff directly (not just the message) — the migration is real and tested, not just claimed. |
| F-187 | DONE | Commit `18b8c2c` — `Today.css` adds `.km-today__sectionTitle--topik { margin-top: 4px; }` (scoped to only the Suggested-learning→TOPIK boundary, per the code comment explaining why the other section gap is deliberately untouched). | Verified by reading the diff directly. |
| F-188 | DONE | Commit `18b8c2c` — `Today.css` adds `.km-today__hub .km-hubheader__rail-divider .km-dancheong-rail { display: none; }` with an extensive code comment identifying the root cause (`DancheongRail`'s card-edge-stripe geometry misapplied inside `PageHubHeader`'s divider slot) and confirming the fix is scoped to Today only (other `PageHubHeader` consumers unaffected). | Verified by reading the diff directly. |
| F-189 | DONE | Commit `18b8c2c` — one canonical 6-skill color map (`client/src/lib/skill-colors.ts`; indigo=Vocab, vermilion=Grammar, ochre=Hanja, cyan=Reading, moss=Listening, violet=Writing), wired to both Today's tile carousels and `LearnMenu.tsx`'s honeycomb via the shared `--km-tone` mechanism; `DancheongRailTone`/`CityCardTone` gain `cyan`/`violet`; `tokensContrast.test.ts` extends its AA regression guard to both new tones in both themes. Further retuned by `0a53bf7`/`b402008` (TOPIK's own dedicated `stone` tone + Night hue retune, tracked as the separately-already-DONE F-191). | Verified by reading the diff + the follow-up commits' messages/stat. |
| F-190 | DONE | Commit `18b8c2c` — `useCenterOnMountRef` (ref-callback + native `scrollIntoView({inline:'center'})`, `Today.tsx`) fires once per mount; Review&Drills carousel reordered to Grammar/Vocab/Hanja (Vocab centered); Suggested-learning reordered to Listening/Reading/Writing (Reading centered). `Today.test.tsx` gains coverage (227 new lines). | Verified by reading the diff directly — both carousels' reorder and the centering mechanism are real, not just described in the commit message. |

## Summary

- **DONE: 44** — F-143, F-144, F-145, F-146, F-147, F-148, F-149, F-150, F-152, F-153, F-154, F-155, F-156, F-157, F-158, F-159, F-161, F-162, F-163, F-164, F-165, F-166, F-167, F-168, F-169, F-170, F-171, F-172, F-174, F-175, F-176, F-177, F-178, F-180, F-181, F-182, F-183, F-184, F-186, B-036, F-187, F-188, F-189, F-190
- **PARTIAL: 2** — F-160 (loader mapping bug fixed + tested, same root cause as F-185; live-DB re-run + 3-episode content gap still pending), F-173 (backend `totalItems` plumbing shipped; Today's client-side SubwayProgress for a resumed TOPIK attempt still not rendered)
- **OPEN: 2** — F-151 (genre enum itself never expanded — F-176's Theme facet is a sibling, not this ticket), F-179 (SwipeCarousel `onChange` prop + Review doc comment both absent; P4, expected)

### Flagged for human judgment (not asserted as clean DONE)
- **F-169** — code (and its own comment) ships "sound-only, no gloss" on the index tiles, framed by the implementer as the fix. The ticket's bug-title phrasing is genuinely ambiguous about which direction (sound-only vs gloss-word) was actually wanted. Recommend Jared eyeball the live index tile before flipping this one.
- **F-167** — shipped as green/amber/neutral-gray (not the ticket's literal green/yellow/red) — a deliberate, documented UX call from the batch-3 fix-pass, not an oversight. Flipping to done is reasonable but the color triad differs from the ticket text.

### Quick-flip list (DONE, no caveats)
F-143, F-144, F-145, F-146, F-147, F-148, F-149, F-150, F-152, F-153, F-154, F-155, F-156, F-157, F-158, F-159, F-161, F-162, F-163, F-164, F-165, F-166, F-168, F-170, F-171, F-172, F-174, F-175, F-176, F-177, F-178, F-180, F-181, F-182, F-183, F-184, F-186, B-036, F-187, F-188, F-189, F-190

### Quick-flip list, with a one-line caveat to carry into the ticket note
F-167 (color triad differs from ticket text — done by deliberate UX substitution), F-169 (direction of the fix vs. ticket title is ambiguous — verify against live app before flipping)
