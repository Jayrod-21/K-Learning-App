# Backlog Re-verification 4 — Backend/data/infra + attempt-logging + Today-content + text-size (16 tickets)

Adversarial re-verification of a slice already marked DONE by a prior grep-based pass
(see `BACKLOG_RECON_1.md`/`_2.md`/`_3.md`). Every ticket below was checked against real
implementing code (routes, migrations, client services, tests) — a touched file or
related symbol was never accepted as proof on its own.

Scope: F-022, F-023, F-083, F-086, F-090, F-095, F-100, F-136, F-137, F-138, F-139,
F-140, F-171, F-172, F-174, B-036.

| Ticket | Verdict | Criteria checked | Evidence (file:line) | Needs-eyes | Notes |
|---|---|---|---|---|---|
| F-022 | CONFIRMED-DONE | Audit actually performed + documented, findings traceable into child tickets | `db/docs/REVIEW_phase2g1_dbinfra.md` — independent DB+security review of migrations 045/047, PASS-WITH-CONDITIONS, ran the km_app privilege-denial matrix including `COPY ... FROM PROGRAM` (RCE vector), asserts test evidence (`db/tests/test_km_app_role.py` 258-274, 6+2 passed live) | false | Audit is substantive, not rubber-stamped: 5 findings (SF-1..3, P-1..5) with file:line citations, secret-scan results, and a coordination section. Findings correctly seeded B-030, F-083, F-088, F-089, F-090, F-092, B-032, B-033, F-091, F-093, F-094 as separate open tickets — exactly what "audit performed" should look like. |
| F-023 | CONFIRMED-DONE | (1) ticketing UI (2) own+anonymous-community split, author hidden but stored (3) timestamped comments (4) edit own ticket (5) type field (6) status field | `db/migrations/048_tickets.up.sql:67-70` (`type` CHECK bug/concern/suggestion/request, `status` CHECK open/in_progress/resolved/closed), `:127` (`ticket_comments.created_at TIMESTAMPTZ NOT NULL DEFAULT now()`); `server/src/routes/tickets.ts:198-222` (`GET /tickets/community` selects no `user_id`, only `is_mine` boolean); `:240-300` (`PATCH /tickets/:id` ownership-scoped + `expected_version`); `client/src/pages/Tickets.tsx:411,465,533,594,795-883` | false | `Tickets.test.tsx` has an explicit `describe('Tickets — anonymity contract (F-023)')` block plus a PATCH-with-expected_version test — real behavioral assertions, not mock-call counting. |
| F-083 | CONFIRMED-DONE | New migration drops the 6 named redundant indexes; exports+drops the 2 named orphan bak tables; deploy-runner-applied (not hand-applied) | `db/migrations/045_hygiene_cleanup.up.sql:66-71` (6 `DROP INDEX IF EXISTS` matching the ticket's exact names), `:89-90` (`DROP TABLE IF EXISTS topik_items_explanation_bak_20260706` / `_followup`); down-migration recreates all 6 indexes verbatim (per `REVIEW_phase2g1_dbinfra.md` P-1, checked word-for-word) | false | Also confirms the documented scope change (the audit's proposed grammar FK was correctly dropped with a well-reasoned rationale in the migration header, not silently lost). |
| F-086 | CONFIRMED-DONE | Bulk px→rem conversion across CSS + inline TSX; guard test exists and is full-tree; conversions are mathematically correct | Independent grep run: `grep -rnE "font-size:\s*[0-9.]+px" client/src --include="*.css"` → **0 matches**; inline `fontSize:` px → only `MockBadge.tsx:73` (dev-only, `null` in `import.meta.env.PROD`) and dynamic (non-literal) values in `Images.tsx:582,671` (decorative, `aria-hidden`/mock-scene only); `client/src/styles/fontSizeUnits.test.ts` walks the whole `src/` tree, ran live → 7/7 passed; `git show 15fcbf3` touches exactly 18 CSS files, matching the recon claim; 11 spot-checked px→rem conversions all exact px÷16 | false | Verified independently rather than trusting the recon doc's numbers — every claimed figure reproduced. |
| F-090 | CONFIRMED-DONE (soft) | A pre-046 attempt-history-gap decision was made and is reflected in shipped behavior | `server/src/routes/topik.ts:832` (comment references pre-046 single-slot model as historical context only); `GET /topik/attempts` (F-104) and the F-078/F-082 UI (`Topik.tsx:521-600`, honest empty state "You haven't completed a mock exam yet...") contain **no backfill/synthetic-row logic** — the shipped behavior is the "accept clean start" option the ticket posed | false | Weakest "done" in the batch: there is no explicit written decision doc — the resolution is implicit (absence of backfill code = the decision). Ticket text explicitly frames this as a product decision to make, and it was made by omission, consistently with one of the two options offered. Flagging as a nuance, not a demotion. |
| F-095 | CONFIRMED-DONE | "+" attach button with real document branch (not just image); auto-name fires exactly once per conversation | `client/src/pages/Chat.tsx` composer menu (~2081-2202) with Camera/Upload-image/Upload-document items; `uploadDocumentFile` (~1407-1480) → `conversationService.uploadConversationFile(convId, file, versionRef.current, ...)`; `triggerAutoName` (~1282-1301) latched via `namedRef`, called from `onDone` (~1552) | false | `Chat.test.tsx` asserts exact call counts: `'triggers nameConversation once...'` (asserts `length === 1`), `'does not re-call nameConversation... after already succeeded'` (second turn, still `length === 1`), `'releases the latch on a failed nameConversation call'`. This is exactly the kind of test that would catch "fires every reply" or "never fires" regressions. |
| F-100 | CONFIRMED-DONE | Stale `nav.ts` comment fixed to reflect Review→Uploads instead of Settings→Uploads | `client/src/lib/nav.ts:288-290`: "reached from Review → Uploads (the library row; F-039 moved the area out of Settings)... Comment re-homed per F-100." | false | Trivial doc-only ticket; fix matches the ask exactly. |
| F-136 | CONFIRMED-DONE | Suggested learning covers R/W/L/TOPIK; reading suggestion rotates DAILY via real date-keyed logic (not per-render random) | `client/src/pages/Today.tsx:660-765` (peek slider surfaces reading/listening/writing) + `:979` (TOPIK carousel); `server/src/routes/plan.ts:325-327` orders reading candidates by `md5($2::text || PLAN_DATE_SQL || source_kind || row_id)`, where `PLAN_DATE_SQL` pins to `(now() AT TIME ZONE 'Asia/Seoul')::date` | false | Genuine per-user, per-day deterministic shuffle — not "random on every render." |
| F-137 | CONFIRMED-DONE | No highlight styling on the TOPIK progress-bar/meta line | `Today.tsx:999-1004` TOPIK meta renders plain `Bilingual` text under `km-today__tileMeta`, no highlight class; grep for "highlight" across `Today.css`/`Today.tsx` returns only comments citing F-137 as the reason none exists | false | — |
| F-138 | CONFIRMED-DONE | EACH tile's progress bar reflects real per-day completed exercises, not visits or mocks — checked per skill | Grammar: `Today.tsx:550-554,605-608` real `/grammar-drill/attempts`. Writing: `:555-559,609-612` real writing-attempts. TOPIK: `:560-564,625-628` real `/topik/attempts`. Hanja: `:568-572,613-616` real `GET /hanja/attempts` (migration 059). Reading: `:573-577,617-620` real `GET /reading/attempts` (migration 060). Listening: `:578-582,621-624` real `GET /ttmik/attempts` (migration 061). All filtered by an `isLocalToday(...)` date check against the real timestamp column, not hardcoded. Vocab tile shows live due-count (not a "completed today" claim) — documented in-code as a deliberate honest omission, not a fabrication. | false | Strongest ticket in the batch — traced migration→route→service→UI for all 6 attempt-backed skills, all real. This directly resolves the doc-vs-code tension the task flagged: BUGS_AND_FEATURES.md's own F-171/F-172 "open" entries are what's stale, not F-138. |
| F-139 | **DEMOTE→OPEN** | "Today.tsx no longer renders a standalone words/vocab tile" | `Today.tsx:10-14` (code comment): *"F-139 had removed the vocab/'words' due-count tile entirely... that was wrong: it deleted a first-class daily-action entry point users actually wanted here. RESTORED, exactly at its pre-F-139 fidelity."* Live tile at `Today.tsx:850-889` ("N cards due", routes to `/learn/vocab?study=due`). `Today.test.tsx:582`: `'Vocab tile is RESTORED as a first-class activity...'` | true | **Real demotion.** The code doesn't just fail to satisfy F-139 — it explicitly documents REVERSING it after a later user-feedback pass (F-190). Current shipped behavior is the opposite of what the ticket asked for. This needs a human call: either reopen F-139, or (more likely, given the in-code rationale) mark it explicitly SUPERSEDED/REVERSED rather than "✅ done" — leaving it tagged done is actively misleading since the literal ask ("drop the words tile") was tried and then undone. |
| F-140 | CONFIRMED-DONE | Hanja is a real navigable carousel tile, not a placeholder; per F-138 has a real daily-progress signal | `Today.tsx:890-919`, `onClick` → `navigate('/learn/hanja')`; `hanjaDoneToday` (`:613-616`) feeds `DoneTodayRow` (`:907-914`) from the real `hanja_attempts`-backed endpoint | false | Depends on F-171's backend, which IS live (see F-171 row) — so F-140's "done" status is not resting on a missing dependency as the doc's own F-171 "open" tag would suggest. |
| F-171 | CONFIRMED-DONE (doc is stale, not the code) | Migration creates a genuine per-attempt/per-day Hanja log; route mirrors `/grammar-drill/attempts`; write path fires on real hanja review; client consumes it into a real Today "done today" count | `db/migrations/059_hanja_attempts.up.sql` (per-attempt columns `user_id`,`card_id`,`char`,`rating`,`correct`,`created_at` + `ix_hanja_attempts_user_created`); `server/src/routes/hanja.ts:801-830` `GET /hanja/attempts`; `server/src/services/cardReview.ts:228-230` INSERT fired inside `POST /hanja/cards/:cardId/reviews` when `logHanjaAttempt===true`; `client/src/services/hanja.ts:326` → `Today.tsx:568-572,613-616,907-914` | false | **BUGS_AND_FEATURES.md itself still marks this 🔴 open** — that line is stale. The migration's own header says "storage-only — no route change ships in this file," which likely froze the doc at the migration-only commit; a later commit (`6d05e93`, "daily-attempt logging, count reconciliation, deep-links") added the route + client wiring on top without the ticket doc being updated. Implementation is real; recommend correcting BUGS_AND_FEATURES.md's status, separately from this reverify pass. |
| F-172 | CONFIRMED-DONE (doc is stale, not the code) — both Reading AND Listening wired | Same 4-part chain verified for BOTH skills, not just one | Reading: `db/migrations/060_reading_attempts.up.sql` → `server/src/routes/reading.ts:710` POST `/reading/attempts` (INSERT `:748`) + `:780` GET → `client/src/services/reading.ts:431,462` → `Today.tsx:573-577,617-620,711`. Listening: `db/migrations/061_listening_attempts.up.sql` → `server/src/routes/ttmik.ts` POST `/ttmik/attempts` (INSERT `:452`) / POST `/iyagi/attempts` (INSERT `:502`) + `:533` GET → `client/src/services/ttmik.ts:154,171,203` → `Today.tsx:578-582,621-624,678` | false | Same doc-staleness pattern as F-171: BUGS_AND_FEATURES.md still shows 🔴 open, but both Reading and Listening are fully wired end-to-end (a partial — e.g. only one skill wired — would have forced a PARTIAL verdict; that isn't the case here). |
| F-174 | CONFIRMED-DONE | Shared `LineChart` gets an optional trend-line prop (F-142 parity); the prop is actually enabled on the 5 skill-carousel chart call sites, not just added-and-unused | `client/src/components/LineChart.tsx` — optional `trend?: boolean` prop (default off) + `regressionTrend()` least-squares helper guarded for `n<3`; `client/src/pages/Progress.tsx:324-332` — skill-carousel `<LineChart ... trend />` call passes it enabled for all 5 charts (Reading/Listening/Vocab/Grammar/Writing) | false | Confirmed the prop isn't dead — it's wired live at every intended call site, closing the exact gap F-142 left open. |
| B-036 | CONFIRMED-DONE | Control writes+persists+rehydrates `[data-text-size]`; root CSS reacts; because of the F-086 sweep the change now has broad, real visual effect, not just a Phase-1-primitive sliver | `hooks/TextSizeProvider.tsx:77-81` (writes `documentElement.dataset.textSize`), `:58-64` (`localStorage["km.textSize"]`, try/catch-guarded) + server `/settings/prefs.textSize` sync (`Settings.tsx:801-821`), `:71-73` (rehydrate on load, pre-mount no-flash `<script>` in `index.html`); `client/src/styles/index.css:547-548` (`:root[data-text-size="sm"]{font-size:93.75%}` / `="lg"]{font-size:112.5%}`) | true (partial) | Shipped in PR #109 (`15fcbf3`) exactly as the task described. Mechanism (write→persist→rehydrate→root %→rem cascade) is fully proven by static code + the passing `fontSizeUnits.test.ts` guard; the one thing static reading genuinely cannot settle is "does S vs L look obviously different on a real phone" — flagging that single sub-point as needs-eyes, not the ticket's core mechanism. |

## Counts

- Total re-verified: 16
- CONFIRMED-DONE: 15 (F-022, F-023, F-083, F-086, F-090, F-095, F-100, F-136, F-137, F-138, F-140, F-171, F-172, F-174, B-036)
- DEMOTE→PARTIAL: 0
- DEMOTE→OPEN: 1 (F-139)
- NEEDS-EYES: 2 (F-139 — product call on whether to reopen or mark reversed/superseded; B-036 — on-device visual-scale confirmation only, mechanism itself is settled)

## Explicit demotions

- **F-139 (Remove "words" tile) → DEMOTE→OPEN.** The ticket asked to drop the vocab/"words" tile from Today. Code shipped that, then a later commit (F-190, driven by direct user feedback on the live mobile site) explicitly reversed it — the vocab tile is restored verbatim at `Today.tsx:850-889`, and both the in-code comment (`Today.tsx:10-14`) and the test title (`Today.test.tsx:582`, "Vocab tile is RESTORED as a first-class activity") say so outright. Marking this "✅ done" is misleading — the ticket's literal ask is not the current state of the app. Recommend either reopening F-139 or (more appropriate given the documented rationale) re-tagging it SUPERSEDED/REVERSED so the backlog doesn't claim something the code visibly contradicts.

## Secondary finding (not part of the requested demotions, but load-bearing for this slice)

BUGS_AND_FEATURES.md's own entries for **F-171** and **F-172** (lines ~1581-1587) are themselves stale — they still read 🔴 open and describe hanja/reading/listening attempt-logging as missing, but migrations 059/060/061 plus their routes and full client wiring into Today (`hanjaDoneToday`/`readingDoneToday`/`listeningDoneToday`) are live and shipped (commit `6d05e93`). This is the reverse problem from the one the mandate warned about (wrongly marked done) — here two tickets are wrongly left marked open despite being done — but it directly matters for this slice because F-138/F-140's CONFIRMED-DONE verdict depends on exactly the infrastructure F-171/F-172 claim is absent. Recommend a follow-up doc correction (flip F-171/F-172 to done) even though it falls outside this pass's DEMOTE mandate.
