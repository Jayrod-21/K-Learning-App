# REVERIFY_3 — Adversarial re-verification, slice: Hanja / flashcards / grammar drills / TOPIK / exam UI / legacy reskins / WordPopover

Scope: F-135, F-155, F-158, F-159, F-163, F-165, F-166, F-167, F-168, F-169, F-170, F-180, F-181, F-182, F-183, F-184, F-186.

Method: for each ticket, read the full `BUGS_AND_FEATURES.md` entry (the real ticket text lives at
lines 1445, 1515, 1528, 1533, 1549, 1557, 1560, 1563, 1566, 1569, 1572, 1629, 1633, 1637, 1647, 1651,
1668 — note the file's own Status column still reads 🔴 open for 16/17 of these; only F-135 was
already flipped in a prior write-back), independently opened the real implementing code (not
trusting BACKLOG_RECON_1/3's citations at face value), confirmed every criterion in the ticket text
against source, and ran the actual relevant Vitest file to confirm a real, currently-passing,
behavior-exercising test exists (not merely present in a file — actually green).

Verdict counts: **17 CONFIRMED-DONE, 0 demoted.** Every ticket in this slice holds up under
adversarial review. All 17 have concrete on-disk code satisfying their full ticket text, and all 17
are backed by a named, currently-passing automated test (`npx vitest run <file>` executed live
during this pass, not assumed from a prior report). Two are flagged NEEDS-EYES for a subjective/
visual or ambiguous-intent call, per the task brief.

| Ticket | Verdict | Criteria checked | Evidence (file:line) | Needs-eyes | Notes |
|---|---|---|---|---|---|
| F-135 | CONFIRMED-DONE | Ticket: "Tasks-title IA cleanup" — the old standalone "Today's tasks"/"오늘의 과제" heading + its own `SwipeCarousel` (a THIRD carousel) is folded into a cleaner IA. | `client/src/pages/Today.tsx:1-60` (module header explicitly documents "TOPIK, Reading … tasks" + "TOPIK" sections into one IA, F-135"); `git show 083388d` diff removes the `<Bilingual en="Today's tasks" kr="오늘의 과제" />` heading + its dedicated `SwipeCarousel`/`TaskCard` block entirely (`-` lines at old :813-835); confirmed **zero** occurrences of "Today's tasks" / "오늘의 과제" remain anywhere in `Today.tsx`/`Today.test.tsx`/`Today.css` today (`grep` returns nothing) | **true** | Design/IA judgment call, not a bug fix — the ticket text is short and the "right" IA is subjective. Code demonstrably removed the old 3rd-carousel "tasks" section and folded its jobs into "Suggested learning" + a `km-today__topikExtra` shortcut row; `Today.test.tsx` (50 tests) passes live. Recommend Jared eyeball the live Today page once to bless the new hierarchy |
| F-155 | CONFIRMED-DONE | Ticket: "PDF mobile swipe (paired with F-130) — swipe/arrow page-turn on the PDF viewer must work on touch." Explicitly re-verified per task brief: does a REAL native non-passive touch handler exist (not just Pointer Events, which the module's own 3-part doc-comment history explains do NOT reliably preempt the browser's native scroll compositor on a genuinely-scrollable box)? | `client/src/pages/UploadViewer.tsx:184-193` (module header explains touch is driven OFF React Pointer Events entirely — `if (e.pointerType === 'touch') return;` — because Pointer Events aren't guaranteed non-passive on a real `overflow:auto` box); `:904-907` — real `addEventListener('touchstart',...,{passive:true})`, `addEventListener('touchmove', onTouchMove, {passive:false})` (the one that must be non-passive to win the axis-lock race), `touchend`/`touchcancel` `{passive:true}` | false | Confirms the exact concern flagged in the task brief — a real, deliberately non-passive `touchmove` listener attached via raw `addEventListener` (not JSX `onTouchMove`, which React/`react-dom` force-passives). Test: `UploadViewer.test.tsx:723-1078` ("F-155 mobile swipe" describe block) dispatches real `touchstart`/`touchmove`/`touchend` events and asserts page-turns + a `preventDefault`-on-axis-lock regression guard (:799). `npx vitest run src/pages/UploadViewer.test.tsx` → 59/59 passed live |
| F-158 | CONFIRMED-DONE | Ticket: "choose a single grammar form and drill it continuously (form-picker → endless drill)." | `client/src/pages/Grammar.tsx:481` (`formTarget` state, distinct from the one-shot `drillTarget` deep-link), `:511-513` (`continuousDrill = formTarget !== null`, wins over a stale deep-link), `:1266-1281` (a real "Drill" `<Button>` on every pattern-list row, `aria-label="Drill {pattern} continuously"`), `:1891` (advancing in continuous mode regenerates the SAME pattern rather than rotating the pool) | false | "Drill" row-action is a genuine, reachable UI control (not buried/orphaned) on every row regardless of Learning/Known state. Test: `Grammar.test.tsx:1974-2087` ("Grammar — F-158 pick a form to drill continuously"). `npx vitest run src/pages/Grammar.test.tsx` → 57/57 passed live |
| F-159 | CONFIRMED-DONE | Ticket: "entering TOPIK shows a semi-transparent Study-vs-Mock chooser popup." | `client/src/pages/Topik.tsx:224-226` (`chooserOpen` lazy-seeded `true` only when the URL has no explicit `?mode=`), `:313-320` (`<Sheet open={chooserOpen} …>` — `Sheet`'s own backdrop, `rgba(6,8,12,.55)` + blur, supplies the semi-transparent scrim, confirmed no extra CSS was needed), `:250-256` (`chooseMode` dismisses the sheet AND drives mode through the same `selectMode` path the header Tabs use — one mode-selection path, not two) | false | Explicit deep links (`?mode=mock`/`?mode=study`) correctly skip the gate (lazy initializer reads the URL once). Test: `Topik.test.tsx:670-731` — 5 tests covering fresh-visit gate, Study pick, Mock pick, Esc-dismiss-defaults-Study, and deep-link-skips-chooser. `npx vitest run src/pages/Topik.test.tsx` → 40/40 passed live |
| F-163 | CONFIRMED-DONE | Ticket: "surface 'AI Prompt' as a top-level Writing choice" (not buried below the compose card). | `client/src/pages/Writing.tsx:198` (`type WritingChoice = TopikWritingRubric \| 'ai_prompt'`), `:201-204` (`CHOICES` array — Q53 / Q54 / `{id:'ai_prompt', label:'✦ AI Prompt'}`, three equal entries), `:801-830` (rendered as a `role="radiogroup"` segmented control at the TOP of the page, above the compose card — not nested inside it), `:836` (compose card body swaps to the generator only when `uiChoice==='ai_prompt'`) | false | Genuinely top-level — a real 3-way `radiogroup`, not a link/toggle buried in a sub-panel. Test: `Writing.test.tsx:575-681` ("AI Prompt" is a top-level choice… unselected by default, generator shown only once picked; segmented control reflects reality on first paint incl. a Today-carried topic landing directly on `ai_prompt`). `npx vitest run src/pages/Writing.test.tsx` → 32/32 passed live |
| F-165 | CONFIRMED-DONE | Ticket: "drawing drill runs an Anki-style right/wrong loop feeding a mastery pool" — distinct from the still-OPEN F-115 (guided/gradable stroke-order overlay). Verified the two do not conflict and F-165 doesn't quietly claim F-115's scope. | `Hanja.tsx:342-345` (`promoteState`: new→practicing→banked, already-banked = no-op), `:354-366` (`buildDrawQueue`: requested char first, then practicing-before-new from the SAME fetched pool, capped), `:2546-2560` (`judgeRight` — promotes + advances queue), `:2562-2570` (`judgeWrong` — re-queues, explicitly NO state write), `:2744-2771` (real Right/Wrong `<button>`s wired to the two handlers); `:2717-2721` — the page's own "About this drill" copy explicitly states stroke-order guidance is NOT available (no corpus stroke data), confirming F-165 is self-judged recall only, never touching F-115's stroke-grading scope | false | Draw drill writes to the SAME three-band `Hanja.state` pool F-167's index colors and F-170's progress bar read — one real signal, not a parallel shadow one. Tests: `Hanja.test.tsx:1098-1130` ("F-165" describe block — right-answer promotion + completion; wrong-answer re-queue with no write). `npx vitest run src/pages/Hanja.test.tsx` → 55/55 passed live (covers this + all other Hanja tickets below) |
| F-166 | CONFIRMED-DONE | Ticket: "Create-list popup + Add-hanja picker." | `Hanja.tsx:1775-1803` (`CreateListSheet` — real `POST` via `createList`, Sheet-based popup, wired at `:1749`); `:2225-2311` (`AddHanjaPicker` — bulk picker, fetches the whole `GET /hanja` pool, multi-select + add, wired at `:2204`, opened via `setPickerOpen(true)` at `:2093`) | false | Both are reachable from real UI triggers, not orphaned components. Test: `Hanja.test.tsx:910` ("F-166: creates a new hanja-kind list…"), `:1328` ("F-166: bulk-adds selected hanja to a list via the Add-hanja picker") |
| F-167 | CONFIRMED-DONE (documented deviation) | Ticket literal text: "Hanja index tiles colored by mastery (green/yellow/red)." | `Hanja.tsx:1013-1016` (`HanjaCell` receives `state={h.state}`); `HanjaCell.tsx:36-38` (`STATE_CLASS` maps banked/practicing/new → `km-hanjacell--{banked,practicing,new}`); `index.css:2315-2317` (border-top reads `--km-mastery-mastered`/`-practicing`/`-new`); `index.css:201-203,332-333` resolve those tokens to `--moss` (jade/mint green), `--ochre`/`--ochre-ink` (**amber**, `--dan-ochre`/`--neon-amber` — NOT yellow), and `--paper-mute` (a muted brown-gray `#6B614D` in Day, blue-gray `#8B96C8` in Night — NOT red) | false | Independently confirmed the actual rendered triad is green/amber/neutral-gray, NOT the ticket's literal green/yellow/red — matches BACKLOG_RECON_3's caveat exactly. This is a deliberate, documented UX call (comment at `:755-758`: a never-studied "new" char shouldn't read as "in trouble" red) captured in the batch-3 fix-pass, not an oversight or partial implementation — every tile IS colored by real mastery state, which is the functional intent of the ticket. Test: `Hanja.test.tsx:1211-1222` ("F-167: index tiles carry the real per-state class the mastery-color override keys off") asserts the real state classes render. CONFIRMED-with-note, not demoted — the substance (index tiles colored by mastery) is fully met; only the literal color-name triad differs, by design |
| F-168 | CONFIRMED-DONE | Ticket: "Index '+'-to-list popup + 'added to list' toast." | `Hanja.tsx:1026-1035` (per-tile `+` button, sibling of — not nested inside — the `HanjaCell` button, avoiding invalid button-in-button HTML); `:1054-1139` (`QuickAddSheet`) with real `toast({message:...,tone:'success'})` calls at `:1102` (add success), `:1106` (409 duplicate, `tone:'info'`), `:1130` (create-and-add success) | false | Real API calls (`seedHanjaCard`+`addHanjaToList`/`createList`), not stubbed. Test: `Hanja.test.tsx:1289-1325` ("F-168: adds a character to a list… shows a confirmation toast"; "reads a duplicate membership… as information, closing the sheet") |
| F-169 | CONFIRMED-DONE — flagged ambiguous per task brief | Ticket (filed as a bug): "index tile label shows the hangul reading/sound, not the gloss word." | `Hanja.tsx:1006-1016` — `HanjaCell` is passed `sound={h.sound}` only, no `gloss` prop, with an explicit code comment: "`HanjaCell` gets `sound` (the hangul reading) only, never `gloss` (the Korean gloss WORD)… simply omitting the prop is the whole fix"; `HanjaCell.tsx` renders its gloss caption only when the prop is passed | **true** | Independently re-read the ambiguity the task brief flagged: the ticket's bug-style title ("index shows X, not Y") could mean either "X is the bug, stop showing X" or "X is missing, ticket wants X". The shipped code — AND its own test, `Hanja.test.tsx:1224-1238` explicitly titled "F-169: index tiles show only the hangul reading, never the gloss word" — both assert sound-only as the CORRECT target state, i.e. the implementer read "sound not the word" as descriptive of the desired end-state (declutter the tile), not the bug. This is a defensible, tested, deliberate reading, but the ticket text alone is genuinely ambiguous the other direction — recommend Jared confirm against the live index tile (gloss words are still visible elsewhere — Today's feature card, the detail sheet — so nothing is lost if this reading is wrong, only a UI decision needs revisiting) |
| F-170 | CONFIRMED-DONE | Ticket: "Live progress bar (Hanja)." | `Hanja.tsx:1450-1462` (`SubwayProgress` over the flashcard study session, tracking real position); `:2699-2709` (a second live `SubwayProgress` on the draw drill, `current={masteredCount}`, `valueText="{n} of {m} mastered"` — recomputed every judged answer, not static) | false | Both instances are wired to real session state (not decorative/frozen). Test: `Hanja.test.tsx:1131-1149` ("F-170: the draw-drill progress bar reflects live mastered count as the session advances") — asserts `aria-valuenow` changes as the session progresses |
| F-180 | CONFIRMED-DONE | Ticket: "Hanja StateChip 'Practicing' tone mismatch (vermilion vs ochre)" — the chip and the index grid must read the same color for "Practicing." | `Hanja.tsx:719-726` — "Practicing" `StateChip` now reads `tone="ochre"` (was `vermilion`), comment at `:755-758` states it deliberately reads the SAME `--km-mastery-practicing` token `HanjaCell`'s index-grid border reads (F-167), not the accent-tracking `--vermilion` | false | Verified both sites (`StateChip` at `:724` and `HanjaCell`'s CSS at `index.css:2316`) resolve to the identical `--km-mastery-practicing` → `--ochre-ink` chain — genuinely the same token, not just a visually-similar duplicate. Test: `Hanja.test.tsx:1242+` ("F-180: the 'Practicing' StateChip carries the fixed mastery tone, not accent-tracking vermilion") |
| F-181 | CONFIRMED-DONE | Ticket: "a right answer on an already-mastered character still increments the 'N of M mastered' label on a no-op reconfirmation — must not." | `Hanja.tsx:2546-2560` (`judgeRight`) — `setMasteredCount((n) => n + 1)` is INSIDE the `if (next !== current.state)` guard, i.e. only fires on a REAL promotion, never on a reconfirmation of an already-banked char (comment at `:2549-2554` states this explicitly) | false | Read the exact conditional — the count-bump and the state-write share one guard, so they can't drift apart. Test: `Hanja.test.tsx:1171-1207` ("F-181: a no-op reconfirmation does not bump the 'N of M mastered' progress label") — asserts the bar stays at `aria-valuenow=1` after a no-op Right, then correctly bumps to `2` on a REAL promotion later in the same session |
| F-182 | CONFIRMED-DONE | Ticket: "add a test for the `promoteState` no-op branch (right answer on an already-banked/mastered char = no write)." | `Hanja.test.tsx:1153-1169` — `it('F-182: a right answer on an already-banked character writes nothing (promoteState no-op guard)', ...)`, asserts `setHanjaStateMock` (the PATCH-state mock) is never called while the character still correctly advances out of the queue | false | Exactly the regression-coverage ask in the ticket — a test that would catch a future double-write or mis-promotion. Confirmed passing live as part of the 55/55 `Hanja.test.tsx` run |
| F-183 | CONFIRMED-DONE | Ticket: "Reskin MockMode.tsx (TOPIK timed-exam body) to Seoul — bring it onto PageHubHeader/CityCard/tokens to match Topik.tsx." | `client/src/pages/topik/MockMode.tsx:70-79` (module header confirms it intentionally has NO own `PageHubHeader` — it renders inside `Topik.tsx`'s shared header/Tabs panel, avoiding a double-header); `:106,113,114` (`CityCard`, `SealStamp`, `SubwayProgress` used throughout — hero cards, section cards, past-paper tiles, live-exam progress, milestone stamp); confirmed **zero** `Topbar` import/usage anywhere in the file | false | The "own PageHubHeader" absence is a deliberate, documented architectural choice (nested under Topik's header), not a missed reskin — every other Seoul device (CityCard/SealStamp/SubwayProgress/tone system) is present throughout. Tests: `npx vitest run src/pages/topik/MockMode.test.tsx` → part of a 67/67 combined pass (with Images.test.tsx) |
| F-184 | CONFIRMED-DONE | Ticket: "Reskin Images.tsx — last legacy flat Topbar page — bring it onto the shared header + character devices." | `client/src/pages/Images.tsx:65,69` (imports `CityCard`, `PageHubHeader`), `:253` (renders `PageHubHeader`, replacing the old bare `Topbar`), `:458,500,639,699` (`CityCard` used throughout — sample rows, recent-scan tile, capture card, detected-word list) | false | Repo-wide `grep -rn "import.*Topbar\|<Topbar"` across all of `client/src` (excluding tests) returns **zero** hits — the `Topbar.tsx` component file itself no longer exists in the tree, confirming Images.tsx really was the last holdout and the migration is total, not partial. Test: `npx vitest run src/pages/Images.test.tsx` → part of the same 67/67 combined pass |
| F-186 | CONFIRMED-DONE | Ticket: "Migrate WordPopover.tsx to the shared tone-aware Sheet — it still renders bespoke `role=\"dialog\"` chrome instead of `Sheet`." | `client/src/components/WordPopover.tsx:56-60` (imports `Sheet`, no longer imports/calls `useModalA11y` directly), `:152` (`<Sheet open onClose={onClose} ariaLabel={data.kr} tone="accent">` wraps the entire popup body), `:300` (closing `</Sheet>` tag) | false | Confirmed no bespoke `role="dialog"` JSX remains — only doc-comment prose describing the retired approach for context. A11y (focus trap, Esc/backdrop, restore-focus, scroll-lock) is now fully delegated to `Sheet`'s shared `useModalA11y`, eliminating the prior double-scroll-lock risk. Confirmed still consumed by all 5 real call sites (`Reading.tsx`, `Chat.tsx`, `Images.tsx`, `Ttmik.tsx`, `topik/MockMode.tsx`). Test: `npx vitest run src/components/WordPopover.test.tsx` → 12/12 passed live |

## Explicit demotions

**None.** All 17 tickets in this slice are CONFIRMED-DONE against their full ticket text, each
backed by a real, currently-passing, behavior-exercising automated test executed live during this
pass (not merely cited from a prior report):

- `Hanja.test.tsx` — 55/55 passed (covers F-165, F-166, F-167, F-168, F-169, F-170, F-180, F-181, F-182)
- `Today.test.tsx` — 50/50 passed (F-135)
- `UploadViewer.test.tsx` — 59/59 passed (F-155)
- `Grammar.test.tsx` — 57/57 passed (F-158)
- `Topik.test.tsx` — 40/40 passed (F-159)
- `Writing.test.tsx` — 32/32 passed (F-163)
- `Images.test.tsx` + `topik/MockMode.test.tsx` — 67/67 passed combined (F-183, F-184)
- `components/WordPopover.test.tsx` — 12/12 passed (F-186)

## Needs-eyes

- **F-135** — "Tasks-title IA cleanup" is a subjective design/IA judgment call. Code demonstrably
  removed the old standalone "Today's tasks" heading/carousel and folded its content into
  "Suggested learning" + a TOPIK shortcut row, and `Today.test.tsx` passes, but whether this IS the
  "right" cleaned-up hierarchy the ticket wanted is a visual call only Jared can bless.
- **F-169** — the ticket's bug-style title ("index shows hangul sound, not the word") is genuinely
  ambiguous about which direction was wanted. The shipped code AND its own test both assert
  sound-only as the deliberate, correct target state (declutter intent), which is a defensible
  reading but not the only one the ticket text supports. Low-risk either way — gloss words remain
  visible elsewhere in the app (Today's feature card, the detail sheet) — but recommend a quick
  glance at the live index tile before this is considered fully closed.

Also carried forward as a documented (not demoted) deviation: **F-167** ships green/amber/
neutral-gray rather than the ticket's literal green/yellow/red — a deliberate batch-3 fix-pass
call (a never-studied "new" character shouldn't read as "in trouble" red), functionally satisfying
"index tiles colored by mastery state." Not flagged NEEDS-EYES since the deviation is already
fully documented in-code and in tests, not something only a human eye can adjudicate.
