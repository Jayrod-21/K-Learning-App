# Backlog Reconciliation — Batch 1 (UI / redesign / Today / mobile family)

Reconciled against `rebuild` branch, 2026-07-15. Method: read each ticket's BUGS_AND_FEATURES.md entry, then grep/read the cited files and git history to confirm actual code state (not just commit-message claims).

| Ticket | Verdict | Evidence (commit/file:line) | Notes |
|---|---|---|---|
| B-017 | DONE | Doc's own Phase-0 note (BUGS_AND_FEATURES.md:1077): "fixture-as-real class fully remediated; only real finding folds into F-042." F-042 itself is 🟢 done (line 809) and `client/src/lib/nav.ts:286-297` shows Uploads live under Review (not a stub). | Header at line 692 still says 🔴 open but body text + F-042 completion confirm resolution. Flip to done. |
| F-077 | OPEN | No commits touch `client/src/pages/Hanja.tsx` for wording rework beyond the Phase 3C-1 functional rebuild (`28e95bd`) and later reskins. BUGS_AND_FEATURES.md itself still records "F-077 ⏳ deferred (reword — discuss)" (line 1300). | Explicitly flagged "discuss" — never actioned. Genuinely open. |
| F-086 | DONE | Commit `15fcbf3` "fix(B-036/F-086): migrate font-size px to rem so text-size setting scales the app". | Confirmed shipped very recently, matches prior context. |
| F-087 | OPEN | `client/src/styles/tokensContrast.test.ts` has category-ink, `--paper-mute`, `--paper-dim`, `--on-vermilion`-on-fill, and focus-ring assertions (lines 98-257) but no test asserting `--vermilion` (or any accent) as text/indicator against `--ink` at the AA text bar. `grep "vermilion.*ink"` → no match. | The specific 2.99:1 mint gap this ticket exists to guard against still has zero test coverage. |
| F-097 | OPEN | `client/src/pages/Progress.css:548-557` — `.km-progress__trendKr` rule still present with an explicit comment: "orphaned by the `<Bilingual/>` rewiring; left for the dead-rule sweep." | The exact orphan named in the ticket is still unswept; no full-sheet sweep has happened. |
| F-098 | OPEN | `client/src/pages/Settings.css` uses kebab-case (`__sched-row`, `__sched-badge`); `client/src/pages/Today.css` uses camelCase (`__tileIcon`, `__sectionTitle`). Drift persists exactly as the ticket describes. | No convention doc or mechanical rename found. |
| F-100 | DONE | `client/src/lib/nav.ts:286-290` — comment now reads "reached from Review → Uploads (the library row; F-039 moved the area out of Settings)... Comment re-homed per F-100." | Explicit "per F-100" marker in the fixed comment. |
| F-102 | OPEN | No in-app link to `/images` found anywhere (`grep -rn "'/images'" client/src --include=*.tsx` → only `App.tsx` route registration and `Images.tsx` itself). `client/src/pages/ReviewLibrary.tsx:19` still documents the gap: "interim 'Scan images' row... leaving `/images` with no in-app entry point (its re-entry home is ticket F-102)". | F-184 (reskin Images.tsx) shipped, but that's styling only — no new entry point was added. |
| F-103 | OPEN | `client/src/pages/ReviewLibrary.tsx:95-101` comment: "The exams shelf (F-042): Mistakes and past TOPIK exams share this [route] → /review/mistakes." No dedicated past-exams page/component exists (`PastExams`/`PastTopik` search → no hits). | Still routes into Mistakes as a shared stub per F-042's sanctioned scope, not the dedicated surface F-103 asks for. F-104 (its backend dependency) is done, but the dedicated page itself isn't built. |
| F-112 | OPEN | `server/src/routes/vocabLists.ts` GET `/:id` (lines 276-370) — the entries query (319-357) selects `v.korean, v.english, v.proficiency` etc. via LEFT JOINs to `vocab_entries`/`kgiu_entries`/`hanja_characters` but never joins `example_korean`/`example_english`. | No example-sentence columns on the list-detail DTO. |
| F-113 | OPEN | `server/src/routes/vocab.ts:219-221` — `DueQuerySchema` only has `limit` (no `list_id`). No `list_id`-scoped due query or bulk "add all to review" found in `MyVocabLists.tsx`/`vocab.ts`. | Global `/cards/due` still returns all-due, list-agnostic. |
| F-121 | OPEN | `client/src/components/ShowMore.tsx:60-76` — the final-reveal focus target is `<span ref={focusCatchRef} tabIndex={-1} className="km-sr-only">` — screen-reader-only (visually hidden), not a visible/scrolled-into-view element. No `scrollIntoView` call in the file. File untouched since the Phase 3C-2 commit (`e781f6a`) that originally filed this follow-up. | Fixes the focus-loss defect (WCAG 2.4.3) but not the visible-focus requirement (WCAG 2.4.7) this ticket specifically asks for. |
| F-129 | DONE | Commit `083388d` message: "Progress: F-128 reskin, F-129 mobile-overflow fix (min-width:0 on ellipsis grid item)... Today: ... F-129/F-130/F-131 mobile/swipe/accent." Code: `client/src/pages/Progress.css:542-545` has an explicit `/* F-129 — defensive: ... min-width: 0 */` comment matching the fix. | Confirmed in both commit and live code, not just message. |
| F-130 | DONE | `client/src/components/SwipeCarousel.tsx:1-220` — extensive touch-handling code (`touch-action: pan-y`, pointer handlers, gesture bookkeeping) predates Wave 2 (mobile-hardening commits `934fd2f`, `5621256`, `c6a4436` from 2026-07-12/13) and was reconfirmed in the `083388d` Today/Progress batch. | Carousel touch/swipe genuinely implemented, not just claimed. |
| F-131 | DONE | `client/src/styles/index.css:89-92` — `--vermilion` is defined as `var(--dan-verm)` and is re-pointed inside `[data-accent="blue"/"mint"]` blocks (lines 473-518). All `:hover` rules using `--vermilion` (e.g. `.km-toast__action:hover`, `.km-btn--ghost:hover`, `.km-review__rating--again:hover`) therefore track the chosen accent. `grep` for hardcoded hex/red in any page/component `:hover` rule → no hits. | Hover states are accent-token-driven, not hardcoded red. |
| F-132 | DONE | Commit `77e076d` "feat(polish): post-beta batch — F-132/174/177/178/180/181/182/186". | Confirmed shipped very recently, matches prior context. |
| F-133 | DONE | Commit `083388d`: "Today: ... F-133 tighten...". `client/src/pages/Today.css` diff (+239/-… lines) in that commit reworks spacing. | Message + large Today.css diff both consistent with a tightening pass. |
| F-134 | DONE | Commit `083388d`: "F-134 inline writing tile". BUGS_AND_FEATURES.md's own Wave-2 follow-up section (F-171) references "Today's ... tiles" post-this-batch without re-flagging F-134, consistent with it shipping. | Recommend a quick manual click-through to confirm the writing tile truly expands in place rather than navigating (not independently grep-verified beyond the commit claim). |
| F-135 | DONE | Commit `083388d`: "F-135 tasks IA". | Message-level confirmation; page structure in `Today.tsx` reworked in the same commit (776-line diff). |
| F-136 | DONE | Commit `083388d`: "F-136 R/W/L/TOPIK suggestions". Code: `client/src/pages/Today.tsx:33,114,924-938` — "Suggested learning carousel — Listening · Reading · Writing" section exists with `aria-label="Suggested learning"`. Confirmed live (not just claimed) via later follow-up ticket F-172, which references "Today's Reading/Listening suggestions (F-136)" as an existing, shipped surface needing only a backend daily-count signal. | TOPIK is handled as its own dedicated tile (carousel 2 per the file's doc comment), not folded into the same rail — matches "R/W/L/TOPIK" as separate carousels, consistent with intent. |
| F-137 | DONE | Commit `083388d`: "F-137 plain TOPIK line". Code: `client/src/pages/Today.tsx:88-93` — TOPIK's tile now reads a dedicated `stone` tone (not the shared accent/vermilion family), explicitly to avoid "highlight" collision with Grammar; further retuned in round-4 (`0a53bf7`, `b402008`). | The "no highlights" requirement is satisfied by giving TOPIK its own non-accent hue rather than literally stripping styling — reads as intentional design resolution, not a literal grep-match, so slightly lower confidence than other DONEs in this batch. |
| F-138 | DONE | Commit `083388d`: "F-138 real per-day attempt counts (grammar/writing/topik)". Confirmed live via follow-up tickets F-171/F-172, which describe Hanja/Reading/Listening as the *remaining* gaps for the same per-tile daily-count feature — implying grammar/writing/TOPIK's counts (F-138's actual scope) already ship. | F-171/F-172 are the honest residual (Hanja/Reading/Listening lack a backend attempt-history endpoint) — correctly separate open tickets, not evidence against F-138. |
| F-139 | DONE | `grep -n "Words tile\|km-today__words" client/src/pages/Today.tsx` → no hits; no "Words" section exists in the current Today page. Commit `083388d` message doesn't explicitly list F-139 but the doc's Wave-2 section (F-139 "drop the words section/tile from Today") matches the observed absence. | Confirmed by absence, not an explicit commit-message hit — flag as DONE with slightly lower confidence than the messaged items. |
| F-140 | DONE | Commit `083388d`: "F-140 hanja tile". Code: `client/src/pages/Today.tsx:16,113,183,568-570` — Hanja is in the core drills carousel, `fetchHanjaAttempts` imported and wired via `useEndpointOrMock('today.hanjaAttempts', ...)`. | Confirmed live in code, not just message. |
| F-141 | DONE | Commit `083388d`: "F-141 collapsible sections (TOPIK-compare default open)". Code: `client/src/pages/Progress.tsx:5,101,379,581-635` — three `CollapsibleTile`-wrapped sections, comment confirms "the TOPIK-compare... default-open, the other two default-collapsed." | Confirmed live in code. |
| F-142 | DONE | Commit `083388d`: "F-142 trend line + emphasized data points on attempt chart". Code: `client/src/pages/Progress.tsx:17-19` doc comment: "a least-squares trend line over the derived Overall series and an emphasized marker..." | Confirmed live in code. Note: F-174 (open, correctly) is the residual — the trend line only landed on Progress's own `TrendChart`, not the shared `LineChart.tsx` used by the 5 skill carousels. Don't conflate F-142-done with F-174-open. |

## Summary counts
- DONE: 17 (B-017, F-086, F-100, F-129, F-130, F-131, F-132, F-133, F-134, F-135, F-136, F-137, F-138, F-139, F-140, F-141, F-142)
- OPEN: 9 (F-077, F-087, F-097, F-098, F-102, F-103, F-112, F-113, F-121)
- PARTIAL: 0

## DONE tickets (for flipping)
B-017, F-086, F-100, F-129, F-130, F-131, F-132, F-133, F-134, F-135, F-136, F-137, F-138, F-139, F-140, F-141, F-142
