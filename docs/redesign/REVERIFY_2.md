# REVERIFY_2 — Adversarial re-verification, slice: Vocab / Library / Grammar / dictionary

Scope: F-144, F-145, F-146, F-147, F-148, F-149, F-150, F-152, F-153, F-154, F-156, F-157, F-161, F-162, F-175, F-176.

Method: for each ticket, read the full BUGS_AND_FEATURES.md entry, opened the real implementing
code independently (not trusting BACKLOG_RECON_1/2/3's citations), confirmed each criterion against
source, and located a real automated test (named below) or explained why none is expected.

Verdict counts: **16 CONFIRMED-DONE, 0 demoted.** All 16 tickets in this slice hold up under
adversarial review — every one has concrete, on-disk code satisfying every acceptance criterion in
its ticket text, and all but one (F-145, CSS-only) are backed by a named automated test that
exercises the exact behavior.

| Ticket | Verdict | Criteria checked | Evidence (file:line) | Needs-eyes | Notes |
|---|---|---|---|---|---|
| F-144 | CONFIRMED-DONE | (1) No leftover `km-grammar__*` classnames render on Vocab; (2) grammar is never a creatable list "kind" on Vocab | `client/src/pages/review/ReviewVocab.css:94-106` (`.km-vocab__state` replaces borrowed `.km-grammar__state`); `client/src/pages/review/ReviewVocab.tsx:303` (`<MyVocabLists kinds={['vocab']}>`); `client/src/components/MyVocabLists.tsx:434` (`kinds.length > 1 ? <radiogroup> : null` — kind picker hidden for single-kind mount) | false | Test: `ReviewVocab.test.tsx:242-270` (`.km-grammar__state` asserted `null`) and `:277-301` (opens the real create-list Sheet, asserts no "Grammar"/"문법" text or radio option reachable anywhere) |
| F-145 | CONFIRMED-DONE | Vocab page doesn't overflow/break on narrow viewports: horizontal-scroll backstop + filters stack + flex rows wrap | `client/src/pages/review/ReviewVocab.css:15-17` (`.km-vocab { overflow-x: hidden; }`), `:47-51` (`@media (max-width: 420px)` collapses the 2-col filter grid to 1 col); `client/src/pages/review/ReviewVocab.tsx:261-262` (`addBanner` row: `flex-wrap: wrap` in CSS + inline `minWidth: 0` on the shrinking child) | **true** | No automated test exists (CSS/media-query behavior isn't practically jsdom-testable) — this is expected, not a gap. Code is real and directly on-point; genuine visual confirmation on a real narrow viewport is the only way to fully close this out |
| F-146 | CONFIRMED-DONE | My Lists renders as a collapsible disclosure, default OPEN (not default-hidden) | `client/src/components/CollapsibleTile.tsx:85,93` (`defaultCollapsed = false` default, `useState(defaultCollapsed)`); `ReviewVocab.tsx` mounts `<CollapsibleTile title=...>` around `<MyVocabLists>` with no `defaultCollapsed` override | false | Test: `ReviewVocab.test.tsx:224-239` — asserts `aria-expanded="true"` on first render, then toggles false/true on tap |
| F-147 | CONFIRMED-DONE | Create-list is a popup (Sheet/dialog), not an always-inline card; scoped to vocab only | `client/src/components/MyVocabLists.tsx` — "New list" button opens `CreateListSheet` (a `<Sheet>`-based dialog), confirmed via `screen.findByRole('dialog', {name:'New list'})` in tests; `ReviewVocab.tsx:832` hardcodes `kind:'vocab'` on `AddToListSheet` | false | Tests: `MyVocabLists.test.tsx:120-158` ("the create form is a Sheet popup, not an always-inline card"; success closes it); `ReviewVocab.test.tsx:360+` |
| F-148 | CONFIRMED-DONE | "This Week" moved from always-inline card to a popup; vocab-only (no grammar suggestions) | `ReviewVocab.tsx:238` (`weekOpen` state), `:313-360` (trigger button → `<Sheet>` containing `<WeeklySuggestions showGrammar={false}>`, lazily mounted so nothing fetches until opened) | false | Test: `ReviewVocab.test.tsx:392-438` — "renders behind a popup" (nothing fetched/rendered until tap) AND "never fetches or renders grammar suggestions" (mocks a grammar-suggestion response, asserts the fetch is never called and no grammar text renders) |
| F-149 | CONFIRMED-DONE | Real visible caption above the Vocab search field (not just an aria-label) | `ReviewVocab.tsx:568-580` — `<Eyebrow className="km-vocab__searchLabel"><Bilingual en="Search for a word" kr="단어 검색"/></Eyebrow>` immediately above `<SearchBox ariaLabel="Search for a word">` | false | Test: `ReviewVocab.test.tsx:379-388` — asserts BOTH the accessible name (`getByRole('searchbox', {name:...})`) AND the visible text node exist |
| F-150 | CONFIRMED-DONE | All-Words/browse dictionary excludes grammar POS rows (어미/조사) at BOTH client and server layers | Server: `server/src/routes/krdict.ts:156` (browse branch) and `:193` (search branch) — `WHERE (part_of_speech IS NULL OR part_of_speech NOT IN ('어미','조사'))`, NULL-safe, folded into the `COUNT(*) OVER()` so pager totals are exact on both branches. Client: `client/src/pages/review/ReviewDictionary.tsx:96-97` (`isGrammarPos`/`GRAMMAR_POS` Set), applied at `:289` and `:370` | false | Tests: `server/tests/routes/krdict.test.ts:221-295` ("F-175 grammar-morpheme exclusion" — excludes 어미/조사 on BOTH branches with exact totals, NULL-safety, 명사/접사 unaffected); `ReviewDictionary.test.tsx:376+` ("excludes grammar entries (F-150)") |
| F-152 | CONFIRMED-DONE | "Bank"/"banked" UI labels fully replaced by Add→Added, with a real "Mastered" state gated on `graduated_at !== null` (not on the add action) | `client/src/pages/review/ReviewGrammar.tsx:453-477` — button renders `SealStamp label="Mastered"` ONLY when `isGraduated` (bank row's real `graduated_at`), else "Added"/"Adding…"/"Add"; grep confirms no literal "Bank" UI string remains anywhere in the file (only internal fn/service names `bankPattern`/`listBanked`/`grammarBank` in comments and imports) | false | Test: `ReviewGrammar.test.tsx:390-471` — asserts a fresh add flips to "Added" (never "Mastered"), a `graduated_at: null` bank row renders "Added", a `graduated_at` SET row renders "Mastered" + the milestone seal, and a 409 ("already added") stays "Added", never promoted |
| F-153 | CONFIRMED-DONE | Grammar list paginated 15-at-a-time via the shared `usePagination` hook (not the corpus-wide list at once) | `ReviewGrammar.tsx:496-499` — `usePagination(rows, {initial:15, step:15, max:Infinity})` (uncapped by design — ~370-row corpus, no reason to stop revealing) | false | Test: `ReviewGrammar.test.tsx:778-820` ("F-153 15-at-a-time pagination") — 22-row fixture: exactly rows 0–14 visible initially, "Show more" reveals rows 15–21 (all 22), control then disappears; separate test confirms a filter change resets the window to 15 |
| F-154 | CONFIRMED-DONE | Square question-number tiles in a grid, grouped/divided by session+date; tap → popup with question/answer/jump-to-explanation | `client/src/pages/Mistakes.tsx:130-148` (`groupSessions` — client-side day+mode grouping), `:184-212` (`CityCard` per session holding the tile grid), `:217+` (`MistakeSheetBody` — question, answer key, "See explanation" reveal + Ask-about-this handoff), wired at `:509-538` | false | Test: `Mistakes.test.tsx:175-237` — "each miss renders as a small square question-number tile, grouped under its own date/session divider"; "tapping a tile opens the Sheet popup with the question, the answer key, and a 'See explanation' reveal"; `:316` confirms the SECOND tile in a multi-tile group shows its own (not the first tile's) content |
| F-156 | CONFIRMED-DONE | "Add to review" seeds 15 per corpus (30 total across 2 corpora), not 100/200 | `client/src/pages/Review.tsx:273` — `const SEED_LIMIT = 15` (comment documents the prior value was 100×2=200), consumed at `:538` inside the per-corpus seed loop | false | Test: `Review.test.tsx:1245-1259` — asserts the success banner reads "Added 15 cards to review," and `initCards` is called with `limit: 15` for BOTH `vocab_2000_beginner` and `vocab_2000_intermediate` (not 100) |
| F-157 | CONFIRMED-DONE | Create-list popup on the Flashcards (Review) page too | `Review.tsx:789-821,960+` — "New list" trigger opens a Sheet-based `CreateListSheet`, sharing `POST /vocab/lists` plumbing with the Vocab page's version | false | Test: `Review.test.tsx:490+` ("F-157: create-list is a Sheet popup — closed by default, opens as a real dialog, and closes on Esc without creating anything") |
| F-161 | CONFIRMED-DONE | Listen (TTMIK/Iyagi) lists use additive show-15/expand, NOT next-page pagination | `client/src/pages/Ttmik.tsx:276` — `LIST_WINDOW = {initial:15, step:15, max:990}` (the 990 is a defensive ceiling far above real corpus sizes ~190/~170 — not a hard page cap; there is no next/prev pager anywhere in the file, confirmed by grep) | false | Test: `Ttmik.test.tsx:338-366` — 40-row fixture: "Showing 15 of 40" initially → "Show more (15)" → 30 of 40 → "Show more (10)" (remainder) → 40 of 40, control disappears. Same pattern for Iyagi (`:981`). Note: the RECON_1 doc's "cap 30" citation for this ticket does not match the actual code (`max: 990`), but the ticket's real ask — replace next-page pagination with show-15 — is fully satisfied regardless |
| F-162 | CONFIRMED-DONE | Returning from a lesson/episode detail to the Listen list restores prior scroll position | `Ttmik.tsx:319` (`useListScrollRestore`, sessionStorage-backed, keyed per corpus off `.km-shell__scroll` since `window` never scrolls in this app's Shell) | false | Test: `Ttmik.test.tsx` "F-162 scroll position preserved on back" (~line 1048) — scrolls to 240px, opens a lesson, returns, asserts `scrollTop === 240`; separate test confirms TTMIK/Iyagi keys don't cross-contaminate |
| F-175 | CONFIRMED-DONE | Server-side WHERE-clause grammar exclusion makes the dictionary pager's "N–M of T" count EXACT (not computed pre-exclusion) | `server/src/routes/krdict.ts:156` (browse branch) + `:193` (search branch, correctly parenthesized so the exclusion ANDs onto the OUTSIDE of the 3-way OR match, not just the last arm) — exclusion is inside the same `COUNT(*) OVER()` window the pager total is drawn from, on BOTH code paths | false | Same test file as F-150: `server/tests/routes/krdict.test.ts:221-295` explicitly asserts an EXACT total (not an over-count) on both the search and browse-all branches, plus NULL-safety and non-grammar-POS rows being unaffected |
| F-176 | CONFIRMED-DONE | `theme` (per-book ~31-value facet, distinct from the 3-value `content_domain`/"genre" enum) is a real filterable facet, server + client, end to end | Server: `server/src/routes/vocab.ts:49` (`theme` zod param), `:112/:141` (WHERE clause using the `ix_vocab_entries_theme_subsection` index), `:196-211` (`GET /vocab/themes` distinct-values endpoint). Client: `ReviewVocab.tsx:439-457` (`fetchVocabThemes` loaded once on mount, not hardcoded), `:602-616` (Theme `FilterSelect`, distinct from the `GENRE_OPTIONS`/`DOMAIN_FILTERS` dropdown above it) | false | Test: `ReviewVocab.test.tsx:562-615` — populates the dropdown from the real `GET /vocab/themes` values (not hardcoded), refetches with the `theme` param independent of the Genre facet, and degrades to "All themes only" if the values fetch fails. Confirmed F-176 and F-151 (genre-enum expansion, correctly still OPEN per BACKLOG_RECON_2/3) are genuinely different facets — not conflated |

## Explicit demotions

**None.** All 16 tickets in this slice are CONFIRMED-DONE against their full, multi-part acceptance
criteria, each with either a real named test or (F-145 only) direct, on-point CSS that a test
harness cannot practically exercise.

## Needs-eyes

- **F-145** (Vocab mobile responsiveness) — the only ticket in this slice with no automated test.
  Code (`overflow-x: hidden`, a `420px` breakpoint collapsing the filter grid, `flex-wrap` +
  `min-width: 0` on the add-banner row) is real and directly targets the ticket's complaint, but
  "does it actually look right on a phone" is inherently a visual judgment call a grep/unit-test
  pass cannot fully close.

## One accuracy note on the prior RECON docs (not a demotion)

BACKLOG_RECON_1.md's F-161 citation says `usePagination(...cap 30)`; the actual code at
`Ttmik.tsx:276` is `max: 990` (a defensive ceiling far above the ~190/~170-row real corpora, not a
30-row cap). This doesn't change the verdict — F-161's actual ask (kill next-page pagination in
favor of show-15) holds regardless of the ceiling's exact number — but it's worth flagging as a
citation error in the prior pass.
