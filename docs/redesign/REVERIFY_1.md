# REVERIFY_1 — Adversarial re-verification of the Today / design-foundation / round-4 Today slice

Scope: 17 tickets a prior grep-based reconciliation (BACKLOG_RECON_1/_2/_3.md) marked DONE.
Mandate: default to skepticism, open the real implementing code, check each acceptance
criterion, try to prove each is NOT actually complete. READ-ONLY pass.

Branch: `docs/backlog-reconcile-2026-07-15` (off `rebuild`). Method: full ticket-text →
concrete criteria → read route/component/CSS/migration + test → verdict.

**Result: 15 CONFIRMED-DONE · 1 DEMOTE→PARTIAL (B-017) · 1 DEMOTE→OPEN (F-134).**

| Ticket | Verdict | Criteria checked | Evidence (file:line) | Needs-eyes | Notes |
|---|---|---|---|---|---|
| B-017 | **DEMOTE→PARTIAL** | (a) no placeholder where real DB data exists; (b) sweep all pages | `client/src/pages/Mistakes.tsx:340-395` `WritingReviewSection()` is a STATIC component ("Static — no fetch happens here", :343) rendering two hardcoded "coming soon" bodies (:365,:379), mounted unconditionally at :540. Its blocker F-106 (`GET /writing/attempts`) **is live**: route `server/src/routes/writing.ts:329-401`, client wrapper `client/src/services/writing.ts:188-192`, already consumed for real at `client/src/pages/Today.tsx:555-558`. `Mistakes.test.tsx:497-518` locks in the stale stub. | false | Recon accepted BUGS_AND_FEATURES.md's own self-report. The named Phase-0 finding (Review/Uploads stub) IS resolved (`lib/nav.ts:286-297`), but Mistakes.tsx is a live counter-example of the exact "designed placeholder where a route now returns real data" class B-017 exists to catch. Real DB data (`writing_attempts`) fetchable + fetched elsewhere, never wired into the page F-046 targeted. |
| F-129 | CONFIRMED-DONE | body never scrolls sideways; wide content own `overflow-x:auto`; covers Progress+Vocab+Grammar | Progress `min-width:0` `Progress.css:542-545`, page guard `:34`, table wrapper `:299`; Vocab `ReviewVocab.css:10-16,63,73`; Grammar `Grammar.css:2,34,69-71,147,173` + `ReviewGrammar.css:43,80`; app-wide backstop `styles/index.css:1129` `overflow-x:hidden` on `.km-shell__scroll` (every page's scroll container). Tests: `Progress.test.tsx:1629-1649`, `Grammar.test.tsx:2258-2286`. | false | Covers all 3 named pages + more (Dictionary/Hanja/Review/SkillsCompare). Caveat: tests are jsdom structural proxies (className/ancestor checks), no Playwright real-viewport pixel test exists — mechanism real, coverage is a proxy. |
| F-130 | CONFIRMED-DONE | swipe works on carousels AND PDF viewer (two separate targets) | `SwipeCarousel.tsx:104-261` real pointer-event state machine (`onPointerDown/Move/Up/Cancel/Leave`, 8px axis-lock, snap threshold, setPointerCapture safety) — not CSS-only. PDF viewer independently: `UploadViewer.tsx:690-786` (pointer) + `:815-914` native `touchstart/move/end` via `addEventListener` with `{passive:false}` (deliberately not JSX onTouch). Tests: `SwipeCarousel.test.tsx:54-70`, `UploadViewer.test.tsx:713-905`. | false | PDF half independently verified, not covered by SwipeCarousel evidence alone. |
| F-131 | CONFIRMED-DONE | hover/active follow `data-accent`, not hardcoded red | `styles/index.css:89-92` `--vermilion:var(--dan-verm)`; re-pointed in `[data-accent="blue"/"mint"]` blocks `:473-518` (+ dark variants). Full-tree sweep of every `:hover`/`:active` rule: only one hardcoded color (`index.css:2368` `.gram-span:hover` = Grammar's dedicated indigo skill color, not the accent system, not a red). Cited hover rules real: `:823,:986,:3650` all use `var(--vermilion…)`. Tests: `LearnMenu.test.tsx`, `Reading.test.tsx` reference F-131. | false | No accent-bypassing red hover found. |
| F-132 | CONFIRMED-DONE | (a) auto-switch by local time wired live; (b) manual override wins + persists | `hooks/theme-context.ts` `resolveAutoTheme(now,getHours)` → light for hour∈[6,18) else dark. `ThemeProvider.tsx:167-175` polls every 60s while `mode==='auto'` (live timer, not dead code); `toggleTheme` `:211-216` comment "a manual override always wins over both dynamic modes (F-132)", persists to `localStorage['km.theme']`. Settings 4-way control `Settings.tsx:1809-1857`. No-flash bootstrap `client/index.html:26-51`. Tests: `ThemeProvider.test.tsx:181,259,296` (auto resolution, manual-wins-past-boundary, pure fn). | false | Recon's weak commit-message-only citation was misleading — real, tested code confirms both halves. |
| F-133 | CONFIRMED-DONE | tightened layout / reduced whitespace on Today | `Today.css:158-162` CityCard padding 20px→`14px 16px` (compound selector wins over CityCard.css); header comment `:3` "tightened layout (F-133)"; section-gap tightening throughout (`:88,:101`). | **true** | "Reduce white space" is inherently subjective — mechanism is real and purposeful, but adequacy needs a look on a phone. |
| F-134 | **DEMOTE→OPEN** | Writing tile expands content IN PLACE, does NOT navigate away | `Today.tsx:759-762` — Writing tile's `onClick` calls `navigate(writingHref(t))` (React Router) → **navigates to `/learn/writing?promptId=…`, a separate page**. Module header `:49-56` + tile comment `:732-739` explicitly state F-134's inline `CollapsibleTile`+`WritingTopicGenerator` expand "does NOT fit this model" and was replaced with a deep-link "same shape as Reading/Listening". | false | The sole acceptance criterion (expand inline, not separate page) is entirely unmet — the inline-expand was deliberately reverted to navigation. No inline-expand render path exists for the writing tile. Hard OPEN, not partial. |
| F-141 | CONFIRMED-DONE | every section collapsible; TOPIK-compare defaults open, others collapsed | `Progress.tsx:586-635` all 3 top-level sections are `CollapsibleTile`: TOPIK-compare `defaultCollapsed={false}` (:588), Progress-by-skill `defaultCollapsed` (:617), Mastery `defaultCollapsed` (:628). `CollapsibleTile.tsx:93-99` real `useState`+`<button>`+`aria-expanded` (not a no-op wrapper). Comment `:581-585` matches. | false | Progress has exactly 3 top-level sections; all 3 collapsible = "everything". |
| F-142 | CONFIRMED-DONE | trend + visible data points on ALL Progress graphs | Diagnostic TrendChart got trend+emphasized-marker in F-142 (`Progress.tsx:17-20`). The 5 skill-carousel charts render via shared `LineChart.tsx` which gained the trend line via **F-174** (`LineChart.tsx:67,174,221,315-336`; consumed `Progress.tsx:324-332`, comment `:329` "F-174 — parity with the diagnostic Trend chart's F-142 treatment"). | false | Current tree DOES show trend+points on all Progress graphs — the user-facing "all graphs" outcome holds. Caveat: F-142's OWN commit (083388d) only covered TrendChart; the 5 skill charts' coverage was completed by the separate (already-merged) F-174. Not a demotion since the deliverable is present, but completeness is split across two tickets. |
| F-143 | CONFIRMED-DONE | remove "begin today's plan" + "gaps/next steps" from Progress | Grep of Progress.tsx: strings appear only in explanatory comments (`:59-62,:641-646`) stating neither block ever existed on Progress (they live on `Diagnostic.tsx`'s results screen). Regression test `Progress.test.tsx:1575-1585` asserts `/Begin today's plan/i`, `/^Next steps$/i`, `/Derived from your gaps/i` are all absent. | false | Verified no-op — the blocks were never on this page; a regression pin, not a feature add. (BUGS_AND_FEATURES.md still shows 🔴 open; recon_3's DONE is correct — code supports the flip.) |
| F-164 | CONFIRMED-DONE | Hanja spacing fix exists + purposeful | `Hanja.css:22` dedicated "F-164 spacing — tightened gutters + vertical rhythm" section with real rules (`.screen.km-hanja > * + * { margin-top:12px }` etc.); `Hanja.tsx:52` comment. Commit `8eae3c8`. | **true** | Ticket text is just "Spacing" (no detail) — code is real and clearly purposeful, but "looks right" can only be confirmed visually. |
| F-177 | CONFIRMED-DONE | both Today+Progress migrated to shared PageHubHeader; exactly one header recipe | `components/PageHubHeader.tsx` exists (title slot + DancheongRail divider). Today renders `<PageHubHeader …>` `Today.tsx:171(import),:792`; Progress `Progress.tsx:107(import),:568`, comment `:561` "migrated to the shared PageHubHeader component per F-177". Sweep: the only `SkylineHeader`/`DancheongRail` mentions in Today/Progress are doc-comment lines (`Today.tsx:782-783`), NOT inline JSX — no leftover inline copy in either page. | false | Exactly one header-recipe location confirmed; no half-migration. |
| F-178 | CONFIRMED-DONE | Today Hanja tile uses `ochre`, off the `plain` fallback | `lib/skill-colors.ts:90` `hanja:{hexHue:'ochre',tone:'ochre'}`. Today Hanja tile `Today.tsx:892,910` renders `tone={SKILL_COLOR.hanja.tone}` (resolves ochre) + `<Pill tone="ochre">` :896. No `tone="plain"` remains on the Hanja tile. | false | Real prop wiring (not just an unused constant); superseded/subsumed by F-189's canonical map, which also lands ochre for Hanja. |
| F-187 | CONFIRMED-DONE | reduce gap ONLY at Suggested-Learning→TOPIK boundary | `Today.css:94-96` `.km-today__sectionTitle--topik{margin-top:4px}` (vs shared 12px `:78`), comment `:87-93` scopes it + says other gap must stay. Applied only to TOPIK `<h2>` `Today.tsx:971` (Review&Suggested headings lack the modifier). Test `Today.test.tsx:1341-1371` (only headings[2] carries class, exactly one element, parses CSS `<12`). | **true** | Mechanism code-proven + correctly scoped; gap-size adequacy is a visual call. |
| F-188 | CONFIRMED-DONE | remove stray blue rail line above Review&Drills; scoped to Today only | `Today.css:60-62` `.km-today__hub .km-hubheader__rail-divider .km-dancheong-rail{display:none}`, root-cause comment `:35-59`. DOM chain real: `PageHubHeader.tsx:73-100` renders `rail-divider`→`DancheongRail`, `Today.tsx:793` passes `className="km-today__hub"`. Selector `.km-today__hub`-scoped → other PageHubHeader consumers untouched (grep confirms no other page uses that selector). Wrapper spacer preserved. Test `Today.test.tsx:1373-1403`. | **true** | Correctly scoped; visual "no more line" needs a look but mechanism is real. |
| F-189 | CONFIRMED-DONE | one canonical map, 6+ distinct hues, SAME map on Today tiles AND LEARN honeycomb, AA both themes | `lib/skill-colors.ts:87-95` `SKILL_COLOR`: flashcards→indigo, grammar→crimson, hanja→ochre, reading→cyan, ttmik→moss, writing→violet, topik→stone (all distinct). Today tiles consume `SKILL_COLOR.<skill>.tone` (`Today.tsx:665,679,698,712,739,753,822,855,892,989,1008`). LearnMenu honeycomb consumes SAME map `LearnMenu.tsx:120(import),:315` `hexwrap--${SKILL_COLOR[navId].hexHue}`. AA test `tokensContrast.test.ts:35-43` HUES incl cyan/violet, `--{hue}-ink` on `--{hue}-soft` ≥4.5:1 in light(:100)+dark(:101). No hardcoded hex in Today/LearnMenu (grep clean). Tests `Today.test.tsx:864`, `LearnMenu.test.tsx:92-124`. | **true** | Wiring/sharing/AA all code-proven and pass. needs-eyes only for the taste call "do 7 colors read as distinct" (guarded objectively by ΔE76 test). Doc nit: recon prose says "vermilion=Grammar" but shipped code uses `crimson` (deliberate earlier fix) — verdict unaffected. |
| F-190 | CONFIRMED-DONE | both carousels open centered on named middle card (Vocab / Reading) | `useCenterOnMountRef` `Today.tsx:518-525` (once-per-mount `firedRef` guard, `scrollIntoView({inline:'center'})`). `vocabCenterRef`→Vocab card `:850` (Review order Grammar:820→Vocab:850→Hanja:890, Vocab=middle). `readingCenterRef`→Reading `:696` (Suggested order Listening:660→Reading:693→Writing:728, Reading=middle). Tests `Today.test.tsx:603-619,621-649,814-824,826-862` — DOM order via accessible names + mocked `scrollIntoView` asserted called once with correct `this` node + negative assertions others NOT centered. | false | jsdom mocks scrollIntoView (expected) but asserts correct node+args — wiring fully proven. |

## Demotions
- **B-017 → PARTIAL** — `Mistakes.tsx:340-395` `WritingReviewSection` is a static "coming soon" stub for TOPIK-writing + generated-prompt responses, yet its only blocker (F-106 `GET /writing/attempts`) is live and already consumed on Today.tsx. Real DB data present, never wired into the page. Exactly B-017's failure class; recon accepted the doc's self-report.
- **F-134 → OPEN** — Today's Writing tile `navigate(writingHref(t))`s to `/learn/writing` (a separate page); inline-expand was deliberately reverted. Sole acceptance criterion ("expands inline, not separate page") entirely unmet.

## Needs-eyes (visual/subjective — best code-verdict given, but confirm on a real phone)
F-133, F-164, F-187, F-188, F-189.
