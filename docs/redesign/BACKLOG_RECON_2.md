# Backlog Reconciliation 2 — backend / data / infra / exam family

Verified against `rebuild` @ working tree (2026-07-15). Method: grep the code
paths named in each ticket's "Key files"/"Where", read the surrounding code,
and cross-check against `db/docs/REVIEW_*` / `FIX_REPORT_*` trail + `git log`.
A ticket ID appearing in a commit message is treated as a hint, not proof —
every row below cites the actual code (or its absence) that was inspected.

| Ticket | Verdict | Evidence (commit/file:line) | Notes |
|---|---|---|---|
| F-006 | OPEN | No `nodemailer`/`sendgrid`/`smtp`/`mailgun` anywhere in `server/src` or `server/package.json` (grep clean). `db/migrations/052_notification_schedules.up.sql` comment: "`notification_deliveries` ... Written by the **FUTURE sender phase**; created empty here." | F-040 (migration 052) built the notification-*intent* store only — no mail transport, no scheduler/worker exists. Ticket's core ask (actual email sending) is still net-new. |
| F-022 | **DONE** | `db/docs/REVIEW_phase2g1_dbinfra.md` (independent DB+security review of migrations 045/047, PASS-WITH-CONDITIONS, ran the km_app privilege-denial matrix incl. the `COPY ... FROM PROGRAM` RCE vector) + sibling `REVIEW_phase2g2_*` / `REVIEW_phase2g3_*` docs. Findings trace directly into shipped tickets: B-030 (superuser role, done), F-083 (hygiene cleanup, done), and residuals correctly spun into their own open tickets (F-088, F-089, F-092). | The audit itself (the ask) was substantively executed across Phase-2; its findings are the reason F-088/F-089/F-090/F-092/B-032/B-033/F-091/F-093/F-094 all exist as tickets in this doc. Never flipped in the summary table despite being the parent of ~10 other rows here. |
| F-023 | **DONE** | `server/src/routes/tickets.ts` (full CRUD + comments), `client/src/pages/Tickets.tsx`, `client/src/services/tickets.ts`, migrations `048_tickets.up.sql` + `058_ticket_source_page.up.sql`. Commit `531d56d feat(tickets): F-023 in-app beta feedback/ticketing client (P1 beta blocker)`. | Author-stored-but-hidden moderation model confirmed in route (`user_id` scoping + no author echoed in DTO comments). Global FAB entry point shipped separately as F-127 (also done). |
| B-025 | PARTIAL | `BUGS_AND_FEATURES.md:1075` (Phase-0 outcomes annotation, already in-repo): "transcripts fully work; read-along **highlight doesn't exist** (no per-line timestamps in corpus) ... decision deferred." | Verification half is done (transcripts confirmed working); the highlight half was confirmed **absent by design gap** (needs forced alignment), not merely unverified — genuinely not built, and the doc itself defers the decision. Not a code fix pending, but not "verified working" either. |
| B-026 | PARTIAL | `BUGS_AND_FEATURES.md:1076`: "root cause: all 58 mp3s (TTMIK L9 5–14; Iyagi 147–150/201–235/237–239/241–246) are **absent from disk**; loader is correct. Repair = user sources the files." | Investigation is complete (root cause found, loader exonerated) — the "investigate" half of the ticket is done. The "restore the audio" half is blocked on the user sourcing missing source files; no code fix applies. |
| F-083 | **DONE** | `db/migrations/045_hygiene_cleanup.up.sql` — drops the 6 named redundant indexes + the 2 orphan `_bak_20260706`/`_bak_followup` tables, exactly as specced. Commit `16a5e10 feat(db): 045 hygiene cleanup — drop 6 redundant indexes + 2 orphan bak tables, add grammar_drill_attempts FK (F-083)`. | Migration header also documents the deliberate scope-cut: the proposed `grammar_drill_attempts` FK was dropped as a wrong finding (attempt-before-bank is legitimate state), which matches this doc's own F-083 note verbatim. |
| B-031 | OPEN (unconfirmed) | `tools/ingest/output/topik_83_I_reading.json` line ~211: `topik83-I-read-042` option[0] = `"수미 씨는 공항에 왔습니다."` — the exact string quoted in the ticket as the OCR glitch, still present. File is **gitignored** (`.gitignore:23`), so there is no commit trail to check whether a local-only patch was applied and reverted, or never applied. | Conservatively OPEN: the text on disk today matches the ticket's description of the *broken* state verbatim, and no corroborating fix record exists (not in `db/docs/`, not in git history since the file was never tracked). |
| F-084 | OPEN | `tools/ingest/output/iyagi_51_100.json` exists (`{"source","units"}`, units numbered 101+ per the ticket) but no loader references it: `grep -rl iyagi_51_100 tools/ingest/loaders/ db/ Deploy/` is empty except `Deploy/parse-corpus.sh` (extraction script, not the DB loader). No reconciliation doc found. | Numbering-mismatch investigation never resolved; file remains unloaded exactly as filed. |
| B-032 | OPEN | `server/src/services/claude/retry.ts:151` still gates on `name === 'APIConnectionError' \|\| name === 'APIConnectionTimeoutError'` — the dead check the ticket describes is unchanged. | No duck-typing on the SDK's actual connection-error shape added. |
| F-085 | OPEN | `.github/workflows/ci.yml` — `node-version: 20` at 3 call sites; `client/Dockerfile` (`FROM node:20-alpine`) and `client/Dockerfile.prod:23` (`FROM node:20-alpine AS build`) unchanged; `Deploy/docker-compose.{blue,green}.yml:156` comments still say `node:20-alpine`; no `"engines"` field in `server/package.json` or `client/package.json`. | Only the server Dockerfile moved to `node:22-alpine` (the original dep-vuln fix) — none of the consistency-sweep items landed. |
| F-088 | OPEN | `db/migrate.py:83` — `DESTRUCTIVE_PATTERNS` regex-based sniffing is still the only mechanism; no per-migration header directive (`-- migrate: destructive`) parsing found anywhere in `migrate.py`. | Unchanged since filed. |
| F-089 | OPEN | `grep -rn "REVOKE.*TEMP" db/migrations/*.sql` — no hits. `km_app` (migration 047) still holds the Postgres default `TEMP` privilege. | Sibling of B-030 (done); this specific hardening step never landed. |
| F-090 | **DONE** (decision shipped implicitly) | `server/src/routes/topik.ts:832` references "the pre-046 single-slot model" only as historical context; `GET /topik/attempts` (F-104) and the F-078/F-082 UIs that consume it contain **no backfill/synthetic-row logic** — they ship the "accept clean-start" option the ticket posed, not a backfill. | Not an explicit written decision doc, but the shipped behavior (no synthetic history rows, history simply starts accruing from 046 onward) is one of the two options F-090 asked to choose between, and it's what's live. Judgment call — flag if a stricter bar is wanted. |
| B-033 | OPEN | `server/src/routes/tickets.ts:261-294` (PATCH `/tickets/:id`) — still: `if (!current) throw new NotFoundError(...)` on the **pre-read** miss, but the UPDATE returning 0 rows still unconditionally `throw new ConflictError('stale ticket version')` (line 294) with no re-probe to distinguish "vanished" (404) from "genuinely stale" (409). | Exactly the bug as filed; no `DELETE /tickets/:id` endpoint exists yet either (consistent with the ticket's "becomes visible the day that ships" framing). |
| F-091 | OPEN | `client/src/components/MyVocabLists.tsx:694` — `key={\`entry:${String(e.entry_id)}\`}` (no `item_type`). `client/src/services/vocab.ts:445-452` — `removeListEntry(id, entryId)` still has no `type` param and the DELETE URL carries no `?type=`. | Server-side is already correct per the ticket; client multi-type awareness still missing. Low current impact (no grammar/hanja add-to-list UI exists yet), consistent with the ticket's own risk note. |
| F-092 | OPEN | `db/migrations/052_notification_schedules.up.sql:155-179` — `notification_deliveries` has no `window_start`/firing-window column and no UNIQUE constraint; only a non-unique `ix_notification_deliveries_schedule_created (schedule_id, created_at DESC)` index for a "newest delivery" probe. | Correctly still open — the sender phase this guards doesn't exist yet either (see F-006), so the race is latent, not live. |
| F-093 | OPEN | `server/src/routes/settings.ts:93-100` — `NotifPrefsSchema` (channel.email/sms + reviewsDue/daily/weekly booleans) is still part of `PrefsSchema`, persisted to `users.preferences` JSONB (migration 018) via `/settings/prefs`. `client/src/pages/Settings.tsx` **separately** syncs `/notifications/schedules` (F-040) in the same screen (lines ~901, ~1134). | Both stores are live simultaneously in Settings.tsx today — exactly the "two sources of truth drift" the ticket warns about. Code comments (Settings.tsx:15-17, 42) describe F-040 as authoritative but the old blob round-trip was never removed. |
| F-094 | OPEN | Private `mapClaudeError` copies still exist, unmigrated: `server/src/routes/grammarDrill.ts:604`, `server/src/routes/diagnostic.ts:1596`, `server/src/routes/conversation.ts:1107`, `server/src/services/imageIngest.ts:407` — all distinct from the shared, corrected `server/src/middleware/errors.ts:128`. The shared helper's own doc comment (errors.ts) says explicitly: "grammarDrill / diagnostic / imageIngest / conversation still carry private flatten-to-502 copies — migrating them is ... tracked as F-094." | Ticket is self-documenting as still-open directly in the code comment. |
| F-095 | **DONE** | `client/src/pages/Chat.tsx` — "+" attach menu fully wired (`attachMenuOpen`/`attachTriggerRef`/`attachItemRefs`, ~lines 999-1016) and a `nameConversation` call at line 1285 (`void conversationService.nameConversation(convId).then(...)`, latched per-conversation ~line 1266). `client/src/services/conversation.ts:158-163` (`nameConversation` → `POST /conversation/:id/name`). | Shipped as part of Phase 3D per `BUGS_AND_FEATURES.md:1394-1396` ("F-035 ✅ '+' attach menu", "F-036 ✅ chats auto-named"). Ticket itself (filed 2026-07-10, predating 3D) was never flipped afterward. |
| F-096 | OPEN | `db/migrations/038_writing_attempts.up.sql` seeds exactly 3 prompts for `topik_ii_53` and 3 for `topik_ii_54` (6 total). No later migration adds writing-prompt content; `056_writing_rubric_widen.up.sql` only widens the rubric CHECK enum (F-117), doesn't seed prompts. | Random-selection backend (B-027/F-117) now exists, but the pool it draws from is still the same shallow ~3/rubric bank. |
| F-099 | OPEN | `client/src/pages/Progress.tsx:1684` — Grammar mastery tab still renders `en="Per-pattern grammar mastery will chart here."` (the designed coming-soon placeholder). No `/grammar/mastery` (or equivalent aggregate) route found in `server/src/routes/grammar.ts`. | F-111 (FSRS schedule folded into `GET /grammar/bank`) shipped separately and is a different surface (per-pattern due state, not an aggregate mastery-bucket read) — does not close this ticket. |
| F-105 | OPEN | `server/src/routes/topik.ts:659-666` — `MistakeRow`/`MistakeDTO` (the `GET /topik/mistakes` DTO, ~line 683) carries `responseId/picked/answeredAt/mode/item` — **no `attempt_id`/`attemptId` field**. (`attempt_id` DOES exist on the separate `GET /topik/attempts` DTO from F-104, but that's a different route.) | Confirmed still missing on the specific route the ticket names. |
| F-107 | OPEN | `client/src/pages/review/ReviewVocab.tsx:13,395-401` — `SavedFromUploads()` component comment: "the backend can't supply this yet" / "Wiring this section to the U3a ... `GET /vocab/saved-from-uploads`". No such route exists server-side; no `source_upload_id` write path on the save/mine endpoints. | Note: `source_upload_id` already exists as a **read-side filter** param on `GET /vocab` and `GET /grammar` (`server/src/routes/vocab.ts:87`, `grammar.ts:56`) — that's a different, pre-existing feature (browse-by-upload), not the save-provenance + saved-from-uploads read this ticket asks for. Easy to mistake one for the other; verified they are distinct code paths. |
| F-108 | OPEN | `server/src/routes/uploads.ts:8` header comment, unchanged: "NO extraction/OCR happens here — U2 (a later, separate phase) is [pending]." No OCR trigger route or pipeline found. | Confirmed still net-new; blocks F-059 (manual OCR button) and F-056 (grammar-from-upload) exactly as the ticket states. |
| F-109 | OPEN | `grep -rn source_format db/migrations/*.sql server/src/routes/uploads.ts` — no hits. No `source_format` column exists. | F-058 shipped its "done-as-respecced" viewable-rendition-filter workaround instead (confirmed correct per its own ticket note); this ticket (the literal filter's prerequisite column) is still open as filed. |
| F-114 | OPEN | `server/src/routes/hanja.ts:132-151` (`mapRowToDTO`) — `id: row.char` (the Hangul character itself is used as `id`; no numeric `hanja_characters.id` selected in `HANJA_SELECT`, lines 163-172). | Unchanged; list-add still needs the disclosed card-seed round-trip workaround. |
| F-115 | OPEN | `client/src/pages/Hanja.test.tsx:1085-1088` — test explicitly asserts "Honest notes: not graded, no stroke-order data, pointer-only" and looks for `/stroke-order/i` text in the UI. `server/src/routes/hanja.ts` has no stroke-path/stroke-order data columns. | Confirmed still an honest freehand-canvas stub, no stroke corpus acquired. |
| F-119 | OPEN | No `audio_path`/audio column on `topik_tests`/`topik_items`; `grep -rn "listening.*audio\|TOPIK.*Listening" server/src/routes/topik.ts tools/ingest` returns nothing serving-side. Only the whole-section source MP3s exist on disk per the ticket's own description (not verified further — out of repo). | Data gap confirmed unresolved: no DTO field, no route, no per-question segmentation. |
| F-120 | OPEN | `server/src/routes/topik.ts:208-312` — DTO still only carries `has_image`/`image_text` (no image URL/asset field); comment at line ~415 still flags "`has_image=true` but NO image asset." | Confirmed still text-description-only, as filed. |
| F-122 | OPEN | `grep -rln topik_level db/migrations/` shows only `005`, `029`, `039` — no `topik_level` column added to `topik_attempts` (migration 037 or later). | F-104/F-118's D-1 level-threading (done) covers the mock-pick/serve/grade path but not the resume/attempt-row persistence this ticket asks for. |
| F-123 | OPEN | `client/src/pages/topik/MockMode.tsx:953-957` — `const done = new Set<number>(); for (const a of res.attempts) { if (a.section === names.kr) done.add(a.sourceTest); }` — keyed by `sourceTest` (test_number) alone, no `topik_level` component. | Exactly the bug as filed: a completed TOPIK II paper's checkmark would also mark the same-numbered TOPIK I paper done. Blocked on F-122 landing the level column, per the ticket's own dependency note. |
| F-124 | OPEN | `server/src/middleware/errors.ts:128-138` (the shared `mapClaudeError`, post-F-094-partial-migration) still does `return new UpstreamError(\`${code}: ${message}\`, ...)` on both the 4xx and 5xx paths — no whitelist/generic-message hardening applied. | Confirmed unchanged; still theoretically safe today only because every proxy error message is a fixed generic string (per the ticket's own risk framing). |
| F-125 | OPEN (deferred by design) | `server/src/routes/conversation.ts:993-1030` (`POST /:conversationId/name`) — still read-then-Claude-then-`UPDATE ... WHERE title IS NULL`, no sentinel/claim column or advisory lock. | This doc's own entry (BUGS_AND_FEATURES.md:1365) explains the deferral rationale in detail (schema change needs a full-suite gate; advisory-lock lifecycle has no precedent in the codebase) — confirmed still the status quo, deliberately not fixed yet. |
| F-126 | OPEN | `Deploy/set-km-app-password.sh:78-87` — verify step still compares against the string literal `'km_app:f'` (line 86: `if [[ "$verify" != "km_app:f" ]]`) while the query (`current_user \|\| chr(58) \|\| (SELECT rolsuper ...)`) concatenates a boolean into text, which Postgres renders as `'false'`/`'true'`, not `'f'`/`'t'`. | Confirmed still the exact false-fail bug as filed; every future deploy will still abort at this step until the literal (or the query) is fixed. |

## Summary

- **DONE: 5** — F-022, F-023, F-083, F-090, F-095
- **PARTIAL: 2** — B-025, B-026
- **OPEN: 27** — F-006, B-031, F-084, B-032, F-085, F-088, F-089, B-033, F-091, F-092, F-093, F-094, F-096, F-099, F-105, F-107, F-108, F-109, F-114, F-115, F-119, F-120, F-122, F-123, F-124, F-125, F-126

### Quick-flip list (DONE)
F-022, F-023, F-083, F-090, F-095
