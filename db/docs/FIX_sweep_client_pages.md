# FIX — SWEEP_client_pages (pages batch: Settings / Grammar / Reference / Login)

Date: 2026-07-06. Branch `fix/bug-sweep`. Scope strictly: `client/src/pages/{Settings,Grammar,Reference,Login}.tsx` + matching `*.test.tsx`. Findings source: `db/docs/SWEEP_client_pages.md`.

| Sweep # | Sev | Disposition | Fix | Regression test |
|---|-----|-------------|-----|-----------------|
| 1 | HIGH | **FIXED** — Settings 409 stranding | `Settings.tsx` `flushSave` catch: on 409 now calls `meQuery.refetch()` (via stable `refetchMe` handle) **in addition to** `refresh()`. `serverVersion` syncs only from `meQuery.data`; refetch → sync effect adopts fresh `version` + profile → next save carries rebased `expected_version`. | `Settings.test.tsx` "rebases the version snapshot after a 409 so the NEXT save succeeds": patch#1 409s with expected_version=1 → asserts `fetchMe` called 2nd time, buffer adopts refetched server truth, patch#2 carries expected_version=2 + succeeds. Fails pre-fix (fetchMe never re-called; expected_version stays 1). |
| 2 | MEDIUM | **FIXED** — Grammar detail-sheet stale-guard | `Grammar.tsx` `openDetail`/`closeDetail`: added `detailIdRef` guard mirroring `Reference.tsx` GrammarTab (`detailIdRef.current !== row.id` → drop settle; guarded `finally`; `closeDetail` nulls it). Bonus hardening: `!row.isReal` branch now resets `detailLoading(false)` so a superseded real-row fetch can't leave a mock row's sheet stuck on "Loading detail…". | `Grammar.test.tsx` "drops a late detail settle for a previously opened row": row A detail deferred, close, open row B (fast), release A late → asserts B's explanation in dialog, A's absent. Fails pre-fix (A's settle paints under B's header). |
| 3 | MEDIUM | **FIXED** — Drill submit-failure Retry wiped the answer | `Grammar.tsx` DrillPanel: the only error `DrillCard` ever renders is a submit failure (generate failures fall to the local mock drill), so the ErrorCard's `onRetry` now re-invokes `submit()` (re-submits preserved answer) instead of `retryGenerate`. Removed now-dead `retryTick` state + `retryGenerate`; generate effect deps `[idx, patternKey]`. Kept the "answer is still here" promise. | `Grammar.test.tsx` "error card's Retry RE-SUBMITS the preserved answer": submit rejects once then resolves; click Retry → asserts `submitDrill` called 2× with identical answer, `generateDrill` still 1×, reveal shows score. Fails pre-fix (Retry regenerated: generate 2×, submit 1×, input wiped). |
| 10 | LOW | **FIXED** — Login RecoveryStep acknowledge locks at "One moment…" | `Login.tsx` RecoveryStep: `void completeEnrollment().catch(noop).finally(() => setFinishing(false))`. Provider probe maps any failure to `guest` and resolves — screen stays mounted, so `finishing` must reset for a retry; on success the screen unmounts and the late no-op setState is harmless (React 18). Defensive `.catch` for future contract drift. | `Login.test.tsx` "re-enables the acknowledge button when the entry probe settles without flipping the gate": deferred `completeEnrollment`, click ack → "One moment…", release probe → button restores to "I saved them — enter" and a second click re-fires (2 calls). Fails pre-fix (button stuck busy forever). |
| 4 | MEDIUM | **FIXED** — Reference Vocab/Grammar tabs swallow pagination/filter fetch errors | `Reference.tsx`: VocabularyTab + GrammarTab render ladders' error branch un-gated from `rows.length === 0` → `error` now always renders `<ErrorCard onRetry>` (mirrors DictionaryTab). Also gated GrammarTab's "N patterns" count caption on `error === null` so it never describes the stale set. Retry re-runs the exact failed fetch (same offset/filters). | `Reference.test.tsx` new describe "browse fetch failures are surfaced, never swallowed": (a) Vocab Next-page fetch rejects → ErrorCard shown, stale rows + "31–60" pager absent, Retry loads real page 2; (b) Grammar level-filter fetch rejects → ErrorCard shown, stale row + "1 pattern" count absent, Retry restores. Both fail pre-fix (rows non-empty → error silently swallowed). |

## Not fixed (out of scope / not defects)
- Sweep #5, #6 (`Review.tsx`), #7, #8 (`Images.tsx`), #9 (`Hanja.tsx`) — other files, not in this batch's file scope.
- Notes item "pages echo `ApiError.message` into ErrorCard" (`Reference.tsx` among others) — sweep classifies it as contract drift, not a defect (React escaping keeps it XSS-safe). Left as-is: fixing only Reference would still leave Ttmik/Progress/Diagnostic/Topik drifting, and several existing Reference tests assert the echoed message text; a coordinated fixed-copy sweep should own it. Flagged for follow-up.
- `RecoveryCodesPanel.tsx` note (missing unmount cleanup for the copy timer) — component file, outside this batch's scope; harmless today per sweep.

## Verify (docker node:20-slim, per batch spec)
- `npx tsc --noEmit` → **TC=0**; `npm run lint` → **LINT=0** (run with this batch's edits; a later re-run showed 1 unrelated error in `client/src/services/vocab.ts` from another agent's concurrent in-flight edit — service files are outside this batch's scope).
- 4 page suites (`Settings/Grammar/Reference/Login.test.tsx`): **91/91 pass** (85 pre-existing + 6 new; the sweep's 5 fixes → 6 tests, Reference got one per tab).
- **Fail-without-fix proven**: reverted the 4 source files to HEAD (tests kept) → exactly the 6 new tests fail, 85 pre-existing still pass; restored fixes → 91/91.
- Full `npx vitest run`: 704/705 — the single failure was `src/pages/Images.test.tsx` (other agent's file, mid-edit at run time); it passes 3/3 when run solo and shares nothing with this batch's files.

---

## Addendum — re-review MED finding: Grammar DrillPanel generate fallback prod-gated (2026-07-06)

Source: `db/docs/REVIEW_SWEEP_client.md` "New findings" MED — the sweep gated "no fabricated data in prod" in `useEndpointOrMock` + MockMode's two inline fallbacks but missed the sibling in `Grammar.tsx` DrillPanel: a failed `POST /grammar/drill` (generate) still served `MOCK_DRILLS` fixtures **with local pseudo-scoring in PROD**, MockBadge suppressed — a fabricated drill scored by `mockScoreFor` and presented as real. Scope strictly: `client/src/pages/Grammar.tsx` + `Grammar.test.tsx`.

| Finding | Sev | Disposition | Fix | Regression test |
|---|-----|-------------|-----|-----------------|
| Re-review MED | MED | **FIXED** — generate fallback prod-gated + Retry rewired | `Grammar.tsx` DrillPanel generate catch: `if (import.meta.env.PROD)` short-circuits before the `MOCK_DRILLS` fallback (mirrors MockMode.tsx:292/354 + useEndpointOrMock.ts:241) → sets new `genError` state (fixed copy `DRILL_GENERATE_ERROR_COPY`, never server prose per ErrorCard contract) + `phase 'ready'`. Panel renders `<ErrorCard onRetry={retryGenerate}>` when `genError !== null` — before the `!item` "Generating drill…" branch. **Retry branches by failure kind:** generate-fail Retry = re-added `retryGenerate` (bumps `genTick`, now in the generate effect deps → re-fires generate for the SAME pattern; it must NOT be `submit()`, whose `if (!item) return` guard dead-ends with `item === null`); submit-fail Retry = unchanged `submit()` inside DrillCard (re-submits the preserved answer — sweep #3's contract intact). Dev/non-PROD fallback (mock drill + 🅂 badge + local scoring) preserved verbatim. | `Grammar.test.tsx` new nested describe "PROD posture — generate failure must error, never fabricate" (3 tests, `vi.stubEnv('PROD', …)` + `vi.unstubAllEnvs`): (a) PROD generate failure → error card with fixed copy, NO textarea, NO mock-badge; Retry → `generateDrill` 2×, `submitDrill` 0×, real drill renders; (b) PROD submit failure → Retry re-submits preserved answer (`submitDrill` 2× identical args, `generateDrill` still 1×, reveal shows score); (c) explicit non-PROD → mock drill + 🅂 badge, no error card. |

### Verify (docker node:20-slim, prescribed gate)
- `npx tsc --noEmit` → **TC=0**; `npm run lint` → **LINT=0** (ESLint strict, set-state-in-effect/refs as errors).
- `src/pages/Grammar.test.tsx`: **32/32 pass** (29 pre-existing + 3 new).
- Full `npx vitest run` → **FULLSUITE=0**.
- **Mutation probe (fail-without-fix):** neutralized the gate (`if (import.meta.env.PROD)` → `if (false)`) → 1 designed failure (the PROD generate-failure test — the error card only the gate renders never appears; the fixture drill does). Restored → 32/32 green, probe grep confirms no remnants. Tests (b)/(c) are polarity guards: (b) pins that the gating didn't re-break sweep #3's submit-Retry contract in prod posture; (c) fails if anyone over-gates the dev fallback.

### Notes
- Sweep #3 removed a then-dead `retryTick`/`retryGenerate` (all DrillCard errors were submit failures — generate failures fell back to the mock silently). The PROD gate makes generate failures a real surfaced state again, so `genTick`/`retryGenerate` return purposefully, owned by the panel-level ErrorCard only; DrillCard's own error stays submit-only and keeps its re-submit Retry.
- `submit()`'s `isMock || attemptId === null` local-scoring branch is now unreachable in PROD (an item only exists there when a real generate succeeded with a real `attemptId`), so no additional gate needed inside `submit`.
