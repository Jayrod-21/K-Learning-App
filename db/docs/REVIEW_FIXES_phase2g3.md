# RE-REVIEW — Phase-2 Group 3 fix-pass verification

**Branch:** `feat/phase2-g3-backend-logic` (fix-pass changes in working tree, uncommitted)
**Re-reviewer:** independent (did not author, did not review originally, did not fix)
**Date:** 2026-07-10
**Inputs:** `REVIEW_phase2g3_{generation,anki,writing_chat,integration}.md` + `FIX_REPORT_phase2g3.md` — every claim below re-verified against the actual code, and the two mutation claims re-derived independently rather than trusted.

## Verdict: **PASS**

All four SHOULD-FIXes from the reviews are genuinely fixed or legitimately deferred with accurate tickets. No regression found. No weakened test found. Both gates green.

## Gates (run fresh by this re-reviewer, full suite alone)

| Gate | Result |
|---|---|
| `npx tsc --noEmit` (server) | **clean (exit 0)** |
| FULL server suite (`npx vitest run`, log `/tmp/km_p2g3_rerev.log`) | **1185 passed / 0 failed / 4 skipped (56 files + 1 skipped), exit 0, 972s** — matches the fix report's 1185/0 exactly; the 4 skips are the opt-in real-API smoke file; the stderr Zod-parse error lines are negative-path fixtures, not failures |
| Independent mutation re-derivation (node simulation of pre-fix docAttach logic on the exact test inputs) | pre-fix emoji doc → **lone surrogate → pg `::jsonb` reject (500)**; pre-fix U+0958 doc → **NFC expands 4000→4001 → maxLength → injection-flavored 400**. Post-fix logic on both → well-formed, ≤4000 units, re-normalization a no-op. The fixer's own mutation log (`/tmp/km_p2g3_mutation.log`) is on disk and shows the same 2 failures on reverted code. |

## Finding-by-finding verification

| # | Review finding | Fix-pass claim | Re-review result |
|---|---|---|---|
| 1a | writing_chat SF-1: `docAttach.ts` surrogate-slice → 500 | FIXED via `truncateAtCodePointBoundary` | **VERIFIED.** `docAttach.ts:163,192-198`: slice to 4000 units, drop a trailing high surrogate (0xD800–0xDBFF). Input is well-formed UTF-16 (strict `fatal:true` UTF-8 decode at :135 cannot emit lone surrogates), so a boundary cut is the only stranding vector — the single-check fix is sufficient, never exceeds the cap. Test literal hex-verified as two real U+1F600 (`f09f9880`), so unit 4000 IS mid-pair; test asserts 201 + exact BMP content + a follow-up send 200 (no wedge). Would fail pre-fix (mechanically confirmed: 500). |
| 1b | writing_chat SF-2: NFC expansion → injection-flavored 400 + false comment | FIXED via normalize→truncate→sanitize reorder | **VERIFIED.** `docAttach.ts:148-167`: NFC first, codepoint-boundary truncate second, `sanitizeUserInput` third — on exactly the text that persists. `sanitize.ts` is byte-identical to `rebuild` (guard NOT weakened; NFC-before-length order at sanitize.ts:96-97 confirmed, so pre-normalizing makes the guard's re-normalization a no-op and its maxLength provably can't fire). The correctness argument holds: NFC composition/canonical-ordering decisions depend only on PRECEDING characters, so a codepoint-boundary prefix of an NFC string stays NFC (mechanically confirmed: re-normalization length-stable on both test excerpts). Injection markers still checked on the persisted excerpt; the marker is still not echoed (generic ValidationError rethrow at :171). Test literal hex-verified as precomposed U+0958 (`e0a5 98`) — with a pre-decomposed literal the test would NOT bite pre-fix, so this detail is load-bearing and correct. |
| 1a/1b semantic change | `truncated` now compares post-NFC length | flagged by fixer as risk-watch | **CORRECT, not a defect.** `truncated = normalized.length > excerpt.length` reports whether the STORED text lost content relative to the normalized document — the honest semantics (test 2 pins it: a 4000-char pre-NFC doc that expands to 4001 loses its combining nukta and is flagged `truncated:true`). Consistent with the wedge-prevention cap: stored excerpt ≤ 4000 UTF-16 units = `ConversationInputSchema` per-turn cap; already-NFC + already-stripped text re-sanitizes idempotently on later sends. |
| 2 | generation SF-1: `mapClaudeError` flattens proxy 4xx → 502 | FIXED for generation routes; shared helper in middleware/errors.ts; F-094 for the 4 other copies | **VERIFIED with one condition (see New findings #1).** `middleware/errors.ts:110-121`: 4xx `httpStatus` passes through via the pre-existing `UpstreamError {status}` override (same mechanism `gradeWriting.ts:160` already used); everything ≥500 still flattens to blanket 502. The proxy hierarchy (`services/claude/errors.ts`, unchanged vs rebuild) has exactly three 4xx classes — `PromptInjectionRejectedError` 400, `ClaudeInputValidationError` 400, `ClaudeRateLimitError` 429 — and ALL are proxy-origin by construction (thrown before/around the SDK call); every Anthropic-origin class (`ClaudeUnavailableError` 502, `ClaudeOutputSchemaError` 502, `ClaudeAuthError` 500, `ClaudePersistenceError` 500) is ≥500 and stays a blanket 502. **Anthropic's status cannot leak through the 4xx branch — SECURITY.md §13.7 honored.** Private copies deleted from `writing.ts`/`reading.ts`; both routes' catches call the shared helper (writing.ts:322, reading.ts:518). Tests: injection→400 + no `generated_stories` row, proxy-limiter→429, both throwing REAL error classes through the stub proxy; the two pre-existing "Claude failure → 502" tests (stub `httpStatus=502`) still present and green — the mapping is proven in both directions. |
| 3 | integration SF-1: Deploy/README missing Group-3 section | FIXED | **VERIFIED.** New section at Deploy/README.md:255, correctly ordered after Group-1 (:127) / Group-2 (:231), mirrors the Group-2 style. Every operational claim checked: 053/054/055 add-only (053 `ADD VALUE IF NOT EXISTS` ×2, 054 new table, 055 enum value + nullable no-default column), unflagged up-deploy (nothing on the way up matches `db/migrate.py:83-85` `DESTRUCTIVE_PATTERNS`), rollback-by-flip valid, `--allow-destructive --target 052` needed only for a schema DOWN across 054's `DROP TABLE`, 055's `DROP COLUMN` correctly described as silent data loss the gate does not match, 053 down no-op, db-backup first, no set-km-app-password (047 default privileges), nginx no-op (existing `/writing` `/reading` `/conversation` prefixes — cross-checked against the integration review's diff of both conf files). The "049/050 downs above" cross-reference resolves (:247). |
| D1 | anki SF-1: "~10 minutes" client banner | DEFERRED → B-034 | **VERIFIED + legitimate.** Present in BUGS_AND_FEATURES.md, accurate (file/line, root cause, `schedule.rating` already on the wire, "B-021 not fully closed until this lands"). Genuinely client-only — the anki review itself said "Not a server defect… deliberately server-only." Not a mis-deferred blocker. |
| D2 | writing_chat coordination: B-027 client wiring | DEFERRED → B-035 | **VERIFIED + legitimate.** Accurate (Writing.tsx rotation cursor + deterministic index; Q53/Q54 header folded in; "do not close B-027"). Client-only by the review's own framing. |
| D3 | F-035/F-036 client slice | DEFERRED → F-095 | **VERIFIED + legitimate.** Phase-3 client work by design (both reviews agree the endpoint contracts are ready); the expensive-limiter NIT is correctly cross-referenced for the name-on-open decision. |
| D4 | prompt content depth | DEFERRED → F-096 | **VERIFIED + legitimate.** Pure DATA ticket (add-only seed migration); correctly notes lengths must stay under the DB CHECK ceilings. |
| R1 | generation NIT-3: relocate IDOR/orphan tests | REJECTED by fixer | **AGREE.** The tests live in `tests/routes/generation.test.ts`, the dedicated suite for this route surface; the review's substantive ask was "name that file in future gate lists" — a process note, honored (the suite was run and extended). Moving tests buys nothing. |

## Regression / weakened-test sweep

- **Fix-pass test changes are purely additive**: working-tree diff on `server/tests/` = 2 new docAttach tests + 2 new status-mapping tests + one header-comment update. Zero assertions removed or loosened.
- **Branch-wide** (`git diff rebuild -- server/tests/`): 1359 insertions / 65 deletions; every deleted assertion is a B-021 FSRS band re-pin (old 10m/1d/3d/6d → new 50s/6m/1d/4d contract) — the intentional retune the anki review verified ("tests pin the NEW contract, the old engine fails them"). Nothing else weakened.
- **App-wide blast radius of the errors.ts change**: additive only — one new exported function; `AppError`/`UpstreamError`/`errorHandler` untouched (diff-verified). Only `writing.ts`/`reading.ts` import the helper. The 4 private copies at `grammarDrill.ts:533`, `diagnostic.ts:1596`, `conversation.ts:1107`, `imageIngest.ts:407` are confirmed present and untouched, so no other route's wire contract moved. Non-proxy errors (AppError carries `status`, not `httpStatus`) pass through `mapClaudeError` unchanged — DB errors inside the same catch still surface as before. Full suite 1185/0 confirms.

## New findings (none blocking)

1. **[NOTE — tracked] Temporary 4xx-mapping divergence until F-094 lands.** The generation review's SF-1 coordination clause asked to fix all copies together *or* extract a shared helper "rather than diverging this route pair." The fixer extracted the shared helper (satisfying the letter) but wire behavior now diverges: injection/proxy-limit reads 400/429 on the generation pair and still 502 on grammar-drill/diagnostic/conversation/image routes. The deferral is honest engineering (each migration is a per-route wire-contract change needing its own suite + status test — F-094 records exact file:line + the test recipe), and the original review itself graded this class "Not a BLOCKER." Condition: F-094 should land in the next backend sweep, not rot.
2. **[NIT — fold into F-094] `gradeWriting.ts:156-160` forwards the RAW `httpStatus` for ≥500 too** (a proxy 500 reaches the wire as 500, where the new shared helper deliberately flattens ≥500 to a blanket 502). Pre-existing, not touched by this fix-pass; when F-094 migrates routes to the shared helper, gradeWriting/enrich should adopt it too and inherit the stricter ≥500 policy.
3. **[NIT — accepted design] 400/429 responses keep `code: 'upstream_error'`.** Slightly odd naming for a client-fault status, but it is the documented UpstreamError contract ("code stays `upstream_error` for a consistent wire shape") and both new tests pin it deliberately. No action.

## Recommendation

**Ready to ship.** Commit the working-tree fix-pass changes and proceed to merge per the normal flow. Follow-ups, in priority order: F-094 (migrate the 4 remaining `mapClaudeError` copies + gradeWriting/enrich adoption — next backend sweep), then the Phase-3 client tickets B-034 / B-035 / F-095 and the DATA ticket F-096 already on the backlog. No further fix-pass needed.
