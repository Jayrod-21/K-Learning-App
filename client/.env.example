# Base URL of the API server (no trailing slash, no /api prefix —
# server mounts routes at /auth, /vocab, /reading, etc.). Cookie auth
# requires this origin to match the server's CLIENT_ORIGIN env var so
# CORS lets the session cookie through.
VITE_API_URL=http://localhost:4000
